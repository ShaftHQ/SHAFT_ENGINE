# SHAFT MCP Tool Catalog

_This file is GENERATED by `scripts/mcp/generate_shaft_skills_tool_catalog.py` from the shaft-mcp @Tool annotations. Do not edit it by hand; re-run the generator instead._

Total tools: 159

Agents should use these exact tool names instead of guessing or listing tools at runtime. Client tool-name prefixes vary: some clients expose `shaft-mcp:<name>`, others expose `mcp__shaft-mcp__<name>` (for example Claude Code). On clients that defer tool schemas until requested, batch-load only the tools you need in a single lookup instead of one at a time, e.g. Claude Code `ToolSearch("select:mcp__shaft-mcp__<a>,mcp__shaft-mcp__<b>")`.

## Autobot (AutobotService)

- `autobot_local_agent_clients` — lists local SHAFT Autobot CLI clients that do not require SHAFT cloud API keys
- `autobot_local_agent_run` — routes Ask, Plan, or approved Agent requests to Codex, Claude Code, or Copilot CLI
- `autobot_provider_chat` — routes Ask or Plan requests to configured SHAFT cloud providers without source mutation
- `autobot_provider_status` — reports the configured SHAFT cloud provider, model, API-key presence (never the value), and structured-output support for the IntelliJ readiness view

## Browser (BrowserService)

- `browser_navigate` — opens a URL in the active SHAFT WebDriver browser session
- `browser_refresh` — refreshes the current page
- `browser_navigate_back` — navigates back to the previous page
- `browser_navigate_forward` — navigates forward to the next page
- `browser_maximize_window` — maximizes the browser window
- `browser_set_window_size` — sets the browser window to a specific size
- `browser_fullscreen_window` — sets the browser window to fullscreen mode
- `browser_delete_all_cookies` — deletes all cookies
- `browser_delete_cookie` — deletes a specific cookie by name
- `browser_get_current_url` — gets current URL
- `browser_get_title` — gets current page title
- `browser_get_page_dom` — returns bounded current-page DOM for locator inspection before element_* or natural_act
- `browser_open_intent` — opens a URL and returns bounded DOM plus capture-ranked locator candidates for the user intent
- `browser_take_screenshot` — takes a PNG screenshot of the current browser viewport
- `browser_storage_state_save` — saves the active browser session's cookies, localStorage, and sessionStorage to a JSON file
- `browser_storage_state_load` — loads cookies, localStorage, and sessionStorage from a JSON file into the active browser session; navigate to the target origin first
- `browser_network_requests` — lists the active browser session's observed network transactions (method, URL, status, mimeType, sizes, timestamp; never bodies); requires the DevTools-based network trace capture that is on by default (shaft.trace.enabled and shaft.trace.includeNetwork)
- `browser_network_request` — returns one observed network transaction's detail (headers and a truncated, redacted response body preview) by the 1-based id from browser_network_requests; full request/response bodies require a capture_start/capture_start_codegen session with saveHarContent=full
- `browser_route` — registers a mock network route on the active browser session, matching by optional HTTP method and a URL glob (Playwright-style * and ?) or an exact URL, and returning the given status/body/headers instead of the real network response; returns a route id for reference (individual removal is not supported -- browser_unroute always clears every route)
- `browser_unroute` — clears registered browser mock routes for the active session; routeId is accepted for reference but every route is cleared because SHAFT's network interceptor does not support removing a single rule yet
- `browser_aria_snapshot` — captures an accessible-name-tree aria snapshot (YAML) of the whole page, or of one element when a locator is supplied
- `browser_accessibility_audit` — runs a non-asserting axe-core WCAG accessibility audit on the current page and returns the violations found; never fails the call when violations exist

## Capture (CaptureService)

- `capture_start` — starts a privacy-safe SHAFT managed-browser recording; while it is active, element_* and natural_act tools drive the recorded browser directly (no driver_initialize needed); sessionGoal names the generated test
- `capture_start_codegen` — starts SHAFT Capture with Playwright-codegen-compatible options; while it is active, element_* and natural_act tools drive the recorded browser directly, so an agent can perform the described actions, capture_stop, then generate code
- `capture_codegen_features` — returns Playwright codegen features and how SHAFT Capture/MCP maps each feature
- `capture_status` — returns SHAFT Capture session, browser, URL, event count, pending debounced-signal count, warnings, and output status; when no session is active the warnings list persisted recordings that can be turned into code without a live session
- `capture_stop` — stops SHAFT Capture; after COMPLETED, show outputPath and generate replay-proven code by passing it as sessionPath to capture_generate_replay (or capture_code_blocks for a faster, unproven draft)
- `capture_api_start` — starts SHAFT Capture with API network recording enabled
- `capture_api_status` — returns SHAFT Capture session status including network transaction count and recent endpoints
- `capture_api_stop` — stops SHAFT API Capture with the same single-session lock guarantee as capture_stop
- `capture_api_transactions` — returns captured network transactions without bodies or sensitive headers; supports filtering asset noise
- `capture_api_generate` — generates, compiles, and returns copy-paste SHAFT.API code blocks from recorded API transactions
- `capture_checkpoint` — records a USER_MARKER, ASSERTION, PAGE_TRANSITION, RECOVERY, FLOW_START, or FLOW_END checkpoint
- `capture_set_mode` — reads or toggles the active recorder's live authoring mode: record (default) or inspect
- `capture_pick_locator` — ranks caller-supplied locator candidates for a picked element and returns a copy-paste SHAFT.GUI.Locator snippet
- `capture_generate_replay` — generates, compiles, optionally replays, and returns copy-paste SHAFT code blocks from a persisted recording JSON (sessionPath); works on any recording file, no active capture session required
- `playwright_capture_generate_replay` — generates, compiles, optionally replays, and returns copy-paste SHAFT Playwright code blocks from a persisted recording JSON (sessionPath); no active capture session required
- `capture_code_blocks` — generates a Java full-class snippet plus agent guidance for repo-aware insertion from a persisted recording JSON (sessionPath); works on any recording file, no active capture session required
- `capture_record_at_target_code_blocks` — generates focused Capture snippets for insertion at an existing Java source anchor
- `playwright_capture_code_blocks` — generates a Java full-class snippet plus agent guidance for SHAFT Playwright insertion from a persisted recording JSON (sessionPath); no active capture session required
- `capture_target_candidates` — suggests existing Java Page Object or test targets for Capture record-at-target insertion
- `capture_backend_comparison` — compares WebDriver and Playwright Capture code-block outputs without editing source
- `capture_evidence_pack` — returns a local manifest of Capture source, report, review UI, screenshots, and checks

## Coding Partner (CodingPartnerService)

- `shaft_coding_partner_plan` — plans a repository-aware SHAFT IntelliJ coding-partner workflow using current source, selected text, existing page objects/tests, capture evidence, and verification guidance
- `shaft_coding_partner_diff` — produces a preview-only unified diff that inserts reviewed SHAFT code blocks into an existing Java target at a chosen method or textual anchor; never writes files

## Doctor (DoctorService)

- `doctor_analyze_failed_allure` — analyzes failed Allure results and returns deterministic actions plus copy-paste code blocks; accepts allure-results directories, individual *-result.json files, or a SHAFT single-file AllureReport.html; when allureResultPaths is empty, automatically analyzes the newest such evidence found in the workspace
- `playwright_doctor_analyze_failed_allure` — analyzes failed Allure results and returns SHAFT Playwright remediation code blocks; accepts allure-results directories, individual *-result.json files, or a SHAFT single-file AllureReport.html; when allureResultPaths is empty, automatically analyzes the newest such evidence found in the workspace
- `doctor_suggest_fix` — returns copy-paste remediation code blocks from an existing SHAFT Doctor report
- `playwright_doctor_suggest_fix` — returns SHAFT Playwright remediation code blocks from an existing Doctor report
- `doctor_propose_healed_locator` — maps a verified SHAFT Heal report to one reviewable locator patch without editing source or publishing

## Element (ElementService)

- `element_hover` — hovers over an element
- `element_click` — clicks an element
- `element_click_js` — clicks an element using JavaScript
- `element_double_click` — double clicks an element
- `element_click_and_hold` — clicks and holds an element
- `element_type` — types value to an element
- `element_append_text` — appends text to an element
- `element_set_value_js` — sets value to an element using JavaScript
- `element_clear` — clears text from an element
- `element_drop_file_to_upload` — drops file to an element to upload
- `element_drag_and_drop` — drags and drops an element from source to target
- `element_drag_and_drop_by_offset` — drags and drops an element by offset
- `element_is_displayed` — checks if an element is displayed
- `element_is_enabled` — checks if an element is enabled
- `element_is_selected` — checks if an element is selected

## Engine (EngineService)

- `driver_initialize` — launches browser
- `driver_quit` — closes browser
- `generate_test_report` — generates a test report for the current session

## Guide (GuideService)

- `shaft_guide_search` — searches the live official SHAFT user guide before writing SHAFT tests, page objects, locators, API, GUI, CLI, mobile, troubleshooting, or best-practice code

## Healer (HealerService)

- `healer_run_failed_test` — reruns a failing SHAFT/Selenium test, analyzes fresh Allure evidence, and returns review-only fixes plus agent replay guidance
- `playwright_healer_run_failed_test` — reruns a failing SHAFT Playwright test, analyzes fresh Allure evidence, and returns review-only fixes plus agent replay guidance
- `verify_run_focused` — runs a guarded, tokenized Maven verification command (compile, test-compile, test, verify, package) headlessly inside the MCP workspace and returns a bounded pass/fail summary; release and deploy goals are rejected

## Mobile (MobileService)

- `mobile_initialize_web_emulation` — starts Chrome or Edge mobile web emulation using SHAFT browser setup
- `mobile_initialize_native` — starts a real Android or iOS native Appium session using SHAFT driver setup
- `mobile_record_start` — starts recording MCP mobile actions to a workspace JSON file
- `mobile_record_status` — returns the active MCP mobile recording status
- `mobile_record_stop` — stops MCP mobile recording and optionally discards the JSON file
- `mobile_api_record_start` — starts a loopback MITM proxy that captures native mobile API traffic to a workspace JSON capture session
- `mobile_api_record_status` — returns the active mobile API capture status, including the CA certificate to install on the device
- `mobile_api_record_stop` — stops mobile API capture, finalizing or discarding the JSON capture session
- `mobile_toolchain_status` — checks local Appium, Inspector, adb, emulator, and SDK tooling status with repair diagnostics
- `mobile_inspector_record_prepare` — prepares a wrapped Appium Inspector mobile recording plan with device and dependency details
- `mobile_inspector_record_start` — starts a confirmed wrapped Appium Inspector recording session
- `mobile_inspector_record_status` — returns the wrapped Appium Inspector recording status
- `mobile_inspector_record_control` — pauses, resumes, checkpoints, stops, or discards a wrapped Appium Inspector recording
- `mobile_inspector_record_stop` — stops a wrapped Appium Inspector recording and returns generated replay code
- `mobile_recording_code_blocks` — generates reusable copy-paste SHAFT mobile replay code blocks
- `mobile_record_at_target_code_blocks` — generates focused mobile recording snippets for insertion at an existing Java source anchor
- `mobile_replay_recording` — replays an MCP mobile recording against the active SHAFT mobile driver
- `mobile_get_contexts` — gets Appium contexts plus current native XML or web DOM source
- `mobile_get_accessibility_tree` — gets the current Appium native accessibility XML tree or mobile web source
- `mobile_natural_act` — performs a trust-gated natural-language touch action by resolving the intent against the mobile accessibility tree
- `mobile_take_screenshot` — takes a PNG screenshot of the current mobile device viewport
- `mobile_switch_context` — switches Appium context, for example NATIVE_APP or WEBVIEW_*
- `mobile_tap` — taps a mobile element using SHAFT touch actions
- `mobile_double_tap` — double taps a mobile element using SHAFT touch actions
- `mobile_long_tap` — long taps a mobile element using SHAFT touch actions
- `mobile_type` — types a value into a mobile element; sensitive fields are redacted per field
- `mobile_clear` — clears a mobile element
- `mobile_swipe_by_offset` — swipes a mobile element by x/y offset
- `mobile_swipe_element_into_view` — swipes the mobile screen until the target element is visible
- `mobile_swipe_text_into_view` — swipes to text using SHAFT Android UiScrollable support
- `mobile_tap_coordinates` — fallback-only: taps viewport coordinates only after locator-based mobile_tap cannot be used
- `mobile_swipe_coordinates` — fallback-only: swipes viewport coordinates only after locator-based mobile_swipe_by_offset cannot be used
- `mobile_rotate` — rotates the mobile device to PORTRAIT or LANDSCAPE
- `mobile_hide_keyboard` — hides the native mobile keyboard
- `mobile_keyboard_key` — sends a native keyboard action such as DONE, SEARCH, GO, NEXT, or SEND
- `mobile_background_app` — sends the active mobile app to the background
- `mobile_activate_app` — activates an installed app by Android package or iOS bundle id

## Natural Action (NaturalActionService)

- `natural_act` — performs a trust-gated natural-language browser, element, or touch action on the active page

## Planner (PlannerService)

- `test_plan_explore` — crawls the active SHAFT browser session breadth-first from a URL, discovering forms, primary navigation actions, and same-origin links, and writes one deterministic Markdown test plan per discovered flow into specs/ (no AI calls)

## Playwright (PlaywrightService)

- `playwright_initialize` — launches a SHAFT Playwright browser
- `playwright_quit` — closes the active SHAFT Playwright browser
- `playwright_record_start` — starts recording MCP Playwright actions to a workspace JSON file
- `playwright_record_status` — returns the active MCP Playwright recording status
- `playwright_record_stop` — stops MCP Playwright recording and optionally discards the JSON file
- `playwright_recording_code_blocks` — generates reusable copy-paste SHAFT Playwright replay code blocks
- `playwright_replay_recording` — replays an MCP Playwright recording against the active SHAFT Playwright driver
- `playwright_browser_navigate` — navigates the SHAFT Playwright page to a URL
- `playwright_browser_refresh` — refreshes the SHAFT Playwright page
- `playwright_browser_navigate_back` — navigates the SHAFT Playwright page back
- `playwright_browser_navigate_forward` — navigates the SHAFT Playwright page forward
- `playwright_browser_set_window_size` — sets the SHAFT Playwright viewport size
- `playwright_browser_new_window` — opens a new SHAFT Playwright tab or window
- `playwright_browser_get_current_url` — gets current SHAFT Playwright page URL
- `playwright_browser_get_title` — gets current SHAFT Playwright page title
- `playwright_browser_get_page_dom` — gets the current SHAFT Playwright page DOM for browser automation inspection
- `playwright_browser_take_screenshot` — takes a PNG screenshot of the current SHAFT Playwright page
- `playwright_browser_storage_state_save` — saves the active SHAFT Playwright session's cookies, localStorage, and sessionStorage to a JSON file
- `playwright_browser_storage_state_load` — loads cookies, localStorage, and sessionStorage from a JSON file into the active SHAFT Playwright session; navigate to the target origin first
- `playwright_element_click` — clicks an element using SHAFT Playwright
- `playwright_element_click_semantic` — clicks an element by human-readable name using SHAFT Playwright
- `playwright_element_click_js` — clicks an element using JavaScript in SHAFT Playwright
- `playwright_element_double_click` — double-clicks an element using SHAFT Playwright
- `playwright_element_hover` — hovers over an element using SHAFT Playwright
- `playwright_element_type` — types value into an element using SHAFT Playwright
- `playwright_element_type_semantic` — types value into an element by human-readable name using SHAFT Playwright
- `playwright_element_append_text` — appends text to an element using SHAFT Playwright
- `playwright_element_set_value_js` — sets an element value using JavaScript in SHAFT Playwright
- `playwright_element_clear` — clears an element using SHAFT Playwright
- `playwright_element_upload_file` — uploads a file through an element using SHAFT Playwright
- `playwright_element_drag_and_drop` — drags and drops an element using SHAFT Playwright
- `playwright_element_is_displayed` — checks whether a SHAFT Playwright element is visible
- `playwright_element_is_enabled` — checks whether a SHAFT Playwright element is enabled

## Shaft Project (ShaftProjectService)

- `shaft_project_create` — creates a new SHAFT Maven project from the same examples and rules used by the guide generator
- `shaft_project_upgrade` — runs the existing SHAFT modular project upgrader script against the current Java project
- `shaft_project_init_agents` — scaffolds SHAFT agent/skill bridge files and a merge-me AGENTS.md guidance file into an existing test repo for a coding-agent loop (claude, codex, opencode, vscode), without overwriting files that already exist unless overwrite=true

## Test Automation (TestAutomationService)

- `test_automation_scenarios` — returns SHAFT MCP usage scenarios, agent actions, design patterns, guardrails, and completion criteria for API, GUI, mobile, capture, Doctor, Heal, reporting, and CI work
- `test_code_guardrails_check` — checks generated SHAFT test code for lexical anti-patterns such as sleeps, brittle locators, raw driver calls, headed setup, direct system properties, and obvious secrets

## Trace (TraceService)

- `trace_latest` — returns recent persisted SHAFT trace indexes from target/shaft-traces
- `trace_read` — returns redacted SHAFT trace JSON from a trace path with explicit output bounds
- `trace_summarize` — returns a deterministic summary of a persisted SHAFT trace without AI
- `trace_open_viewer` — resolves (extracting from the trace ZIP if needed) the offline SHAFT Trace Report HTML viewer for a persisted trace
- `report_merge_shards` — merges N per-shard Allure/trace/doctor-intelligence blobs into one Allure result set plus a flaky-clustering speedboard HTML
- `doctor_analyze_trace` — analyzes a persisted SHAFT trace and returns deterministic Doctor remediation guidance
