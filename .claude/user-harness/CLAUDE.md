<!-- Managed by the SHAFT user harness. -->

# Global agent harness

Before every task, follow the repository's source-controlled `AGENTS.md`; it
selects that repository's local entrypoint, working method, capability level,
skills, MCPs, and knowledge sources.

This generic host file is deployed from a source-controlled harness.
Repository skills stay in their repository. Secrets, credentials, sessions,
caches, indexes, and other runtime state stay local and are never synced.
