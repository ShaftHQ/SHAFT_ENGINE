The trace starts at ConfigLoader.requireValue, called by DriverFactory.create, then TestSession.start. Identify the root owner and first failing operation.

Write only `/app/answer.json` with exactly two string fields: `finding` and `evidence`.
