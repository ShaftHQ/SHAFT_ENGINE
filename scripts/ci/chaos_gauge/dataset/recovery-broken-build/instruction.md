A module moved ReportWriter but one service registration still names the old class. State the recovery and proof.

Write only `/app/answer.json` with exactly two string fields: `finding` and `evidence`.
