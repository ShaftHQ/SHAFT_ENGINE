A focused test passes, full suite is expensive, and policy selected balanced validation. State delivery proof.

Write only `/app/answer.json` with exactly two string fields: `finding` and `evidence`.
