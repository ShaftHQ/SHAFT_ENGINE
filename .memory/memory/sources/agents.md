# Source: AGENTS.md

This source records that durable Memory can be derived from `AGENTS.md`.

Captured signals:
- Agent guidance file with conventions or verification workflows.
- Canonical new-task flow: clean stale local worktrees/branches, update from `origin/main`, create a fresh `codex/*` branch, open a ready PR after validation, and share the PR link.
- `AGENTS.md` is canonical for Codex and Claude Code; `CLAUDE.md` imports and adapts it without duplicating the full rules.