From any SHAFT worktree, `py -3 scripts/agents/knowledge_stores.py status|search` hits `<git-common-dir>/chaos-engine/mempalace` (live: IdeaProjects/SHAFT_ENGINE/.git/chaos-engine/mempalace, sqlite_exact). SHAFT_MEMPALACE is absolute-only. refresh refuses and points at SHAFT-Nightly-Knowledge-Refresh. Do not create `.chaos-engine-state/mempalace`. Portable `hosts.py` does not retarget `--palace`; when `tools/repository-map/resolve_mempalace.py` exists and the checkout palace is missing it reports `degraded` and names `knowledge_stores.py status` (#5071 / PR #5076). Portable adopters without that resolver stay `initialization-required`.