get_health score components: embed_coverage_score 35 (near-max, coverage 0.989), no_dead_links_score 10 (max), link_density_score 0, no_orphans_score 0, timeline_coverage_score 0 -- summing to 45. The zero-scoring components are structural, not a bug: 1143 of 1602 pages are type `code` (auto-ingested SHAFT_ENGINE symbols), which are navigated via code_refs/code_callers/code_callees, not markdown wikilinks -- link_count is 2 across the whole brain. Fabricating wikilinks between code pages to move the score would be metric-gaming, not real remediation. Confirms and supersedes the 2026-07-18 gbrain-2026-07-18-re-spike decision's brain_score figures (same 45/100, same drivers) -- no change since that spike; only new info this pass is the PGLite-lock root cause for why embed-backfill/sync_failures can't self-heal.