True of the maintainer's Windows machine only; re-verify paths before relying on them. Maven builds of SHAFT_ENGINE need JDK 25 (root pom pins jdk.version=25; the default JAVA_HOME openjdk-25.0.1 is correct -- forcing JDK 21 fails with 'release version 25 not supported'). shaft-intellij Gradle needs JDK 21: `C:\Users\Mohab\.jdks\ms-21.0.11` (fallback ms-21.0.9); `C:\Program Files\Java\jdk-21` is a broken install (missing lib\tzdb.dat). Invoke gradle with both JAVA_HOME and -Dorg.gradle.java.home set to the JDK21 path; system Gradle 9.0.0 at `C:\Users\Mohab\AppData\Local\Programs\Gradle\gradle-9.0.0`; never the IntelliJ-bundled JBR (JDK 25). Consumer E2E project: `C:\Users\Mohab\IdeaProjects\shaft-first-time-user` (passing LoginTest, deliberately failing BrokenLocatorTest, recordings/ + generated-tests/ + shaft-skills installed) -- reusable for first-touch/live validation. Local `.m2` 10.3.20260710 shaft-capture/shaft-engine jars are deliberately shadowed with patched builds, so `mvn -pl shaft-mcp test` can fail to compile against stale `.m2` -- fix WITHOUT `-am` or install by listing the dependency modules in the same reactor (`mvn -pl shaft-capture,shaft-doctor,shaft-engine,shaft-mcp test -Dtest=...`). Docs repo `C:\Users\Mohab\IdeaProjects\shafthq.github.io` default branch is `master`. Agent CLIs installed: claude (WinGet Links), codex (npm; codex.cmd resolves); copilot NOT installed.