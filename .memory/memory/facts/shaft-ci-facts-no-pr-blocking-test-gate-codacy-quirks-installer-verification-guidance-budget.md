No PR-blocking Maven test gate: e2eTests/e2eLocalTests/shaft-mcp workflows are schedule/dispatch-only. PR gates are pr-gate.yml's required 'PR Gate Summary' check (path-conditioned legs incl. dependency-review, installer verification 3-OS, intellij build, capture E2E, cli, docs-boundary, template-coupling) plus security.yml (CodeQL only) and shaft-pilot-release.yml. 'Mirror CI locally' = enforcer + convergence + compile (`scripts/ci/local_gate.py`), not test suites. Codacy is org-level (no in-repo config), D212+D213 both on (use single-line Python docstrings + `#` comment blocks); it gates only >=medium issues (0 allowed) and reports as check state `ACTION_REQUIRED`; findings come via check-run ANNOTATIONS (`gh api repos/ShaftHQ/SHAFT_ENGINE/commits/<sha>/check-runs` -> Codacy id -> `/annotations`), not inline review comments. Java PMD to preempt: NPath complexity >200 (keep methods flat -- extract each conditional branch into a helper returning "" so a collector filters); never `setAccessible(true)` in tests (add package-private test accessors); `JUnitTestsShouldIncludeAssert` fires when the only asserts sit inside a lambda/closure (keep >=1 assert in the method body). maven-enforcer-plugin (root pluginManagement, activated in shaft-engine/pom.xml) is the cheap local mirror of merge-time breakage. Installer verification (pr-gate.yml's installer-verify job, 3-OS matrix, path-filtered on intellij/infra changes -- not every PR) runs the real public installer against live Maven Central (`scripts/ci/verify_shaft_mcp_installer_release.py`) -- it verifies RELEASED artifacts, so release-coupled checks must gate on artifact existence (probe `maven-metadata.xml`), not assume it. Guidance budget: AGENTS.md <=6528 bytes, CLAUDE.md <=1536 B/40 lines; every guidance edit must pass `py -3 scripts/ci/validate_agent_setup.py` (NOT a PR gate -- run it yourself); reduction floor 55%; a cross-file duplicate_paragraph check (>=180 chars) machine-prevents gross duplication; CLAUDE.md/AGENTS.md are hand-authored (the refresh-agent-instructions workflow was retired and deleted in PR #3836; agent-instruction refresh is now manual only, no CI-invoked audit gate). A local validator run flags gitignored `graphify-out/GRAPH_REPORT.md` as a documentation-boundary failure -- a local-artifact false positive absent from CI's clean checkout.