Global ~/.claude/settings.json env.ANTHROPIC_BASE_URL=http://127.0.0.1:8787 is correctly wired, but this managed session's process env showed ANTHROPIC_BASE_URL=https://api.anthropic.com - the harness pins its own value, so headroom /stats api_requests stays 0 for such sessions. Only sessions that inherit the global settings env route through the proxy. Do not read api_requests=0 as a broken install.