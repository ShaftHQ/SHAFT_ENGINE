shaft-doctor CAN use shaft-engine public classes directly. The dependency chain is shaft-doctor -> shaft-pilot-core -> shaft-engine, all compile scope, so shaft-engine is on shaft-doctor's compile classpath transitively (`ShardMerger`'s neighbours already import `com.shaft.driver.SHAFT`). Proven by the #3534 speedboard theme convergence (PR #3577): `ShardMerger.writeSpeedboard()` now calls `com.shaft.tools.internal.support.ReportHtmlTheme.style()` directly -- no class move, no new dependency -- with a `ShardMergerTest` drift-guard. An earlier claim that this convergence was 'blocked by a module boundary' was WRONG. Before declaring any shaft-doctor<->shaft-engine work blocked, check the transitive graph. Note: shaft-engine's shade/assembly config EXCLUDES shaft-pilot-core and shaft-doctor from its shaded artifact (pom.xml ~686-688) -- that is jar packaging, NOT a compile-dependency statement, and does not block the reverse (doctor using engine).