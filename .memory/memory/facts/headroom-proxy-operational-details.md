Proxy runs at http://127.0.0.1:8787 (health: GET /readyz or /health, live stats: GET /stats). Managed via headroom install status|apply|unwrap (profile 'default', preset persistent-task, scope provider, backend anthropic, mode token). Two Windows Scheduled Tasks keep it alive across reboots/logons: headroom-default-startup and headroom-default-health, both invoking the ensure-headroom.cmd script under the user's .headroom deploy directory. Actual running processes: a supervisor (headroom install agent run) that keeps a child proxy process (python -m headroom.cli proxy) alive. Global config touched: ~/.claude/settings.json (env.ANTHROPIC_BASE_URL); pre-change backup at ~/.claude/settings.json.pre-headroom.bak. Telemetry explicitly disabled (--no-telemetry, confirmed in the live process command line) -- default is ON (5-min aggregate-only beacon to a Supabase endpoint) so any future reinstall/upgrade should keep passing this flag.