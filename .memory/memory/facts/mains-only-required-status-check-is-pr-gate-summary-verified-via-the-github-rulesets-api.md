Verified 2026-07-26 (re-verified independently against live GitHub, not just the orchestrator's report): `gh api "repos/ShaftHQ/SHAFT_ENGINE/rules/branches/main" --jq '[.[]|select(.type=="required_status_checks")|.parameters.required_status_checks[].context]'` returns `["PR Gate Summary"]` -- that is the only required status check gating a merge to main. The classic branch-protection endpoint (`gh api repos/ShaftHQ/SHAFT_ENGINE/branches/main/protection`) DOES return a 200 with real content (required_pull_request_reviews, enforce_admins, allow_force_pushes, etc.) but its response has no `required_status_checks` key at all -- protection here is configured via GitHub rulesets, not classic branch protection, so `/rules/branches/main` is the endpoint that actually answers 'what status check gates this merge', not `/branches/main/protection`. Easy to wrongly conclude 'no required check configured' from the classic endpoint's silent omission rather than an error.