legacy-shaft-engine/pom.xml publishes artifactId SHAFT_ENGINE purely as a relocation POM ('SHAFT Engine moved to io.github.shafthq:shaft-engine'). Both coordinates resolve on Maven Central and both report the same version (10.3.20260727 as of 2026-07-28), and the shields.io maven-central badge endpoint renders a valid version for EITHER spelling -- so a wrong-coordinate badge does not look broken. Only the central.sonatype.com artifact page reveals the difference. Any user-facing 'check the current version' badge or link must use shaft-engine (shaft-engine/pom.xml:10); SECURITY.md used SHAFT_ENGINE until PR 4295.