e2eTests.yml triggers ONLY on schedule (nightly cron) and workflow_dispatch (.github/workflows/e2eTests.yml:7-10), so a shields.io badge scoped with branch=main on it never reflects main's health. pr-gate.yml is, per its own header comment, 'the single path-conditioned gate for this repository' and triggers on pull_request plus push to main -- it is the correct target for a README build badge. Fixed in PR 4295.