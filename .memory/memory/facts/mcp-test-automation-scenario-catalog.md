`shaft-mcp` exposes `test_automation_scenarios` for LLM agents to retrieve structured SHAFT automation playbooks and `test_code_guardrails_check` to reject generated Java with `Thread.sleep(...)` or absolute `By.xpath("/...")`. API scenario guidance keeps reusable request builders and response validators outside test classes; GUI/mobile guidance prefers POM/fluent SHAFT code and smart/semantic locators before stable `By`/`AppiumBy` locators.