Free-text `/codegen` prompts intentionally route to `capture_start` after #4675, so `AssistantGeminiLiveE2ETest` must not use `/codegen` to reach `autobot_provider_chat`. The live provider test uses a neutral exact-marker prompt and requires the trimmed answer to equal `SHAFT_GEMINI_LIVE_OK`; deterministic routing tests separately own the `/codegen -> capture_start` contract. Run 31459476260 / issue #4697 exposed the stale mixed responsibility when the live test expected provider chat but received capture_start.