PR #4343 (closes #4327, fast-follow to #4321/PR #4326) extends the zero-match self-heal mechanism to Actions.java's MULTI-match branch: when a locator matches 2+ elements and #4321's last-resort visibility narrowing (uniqueDisplayedAndEnabledElement) still leaves 0 or 2+ actionable candidates, HealingManager.resolve() is now consulted (same signature/params as the zero-match branch's findAllElements call) before throwing MultipleElementsFoundException. Both the initial lookup (Actions.java ~514-527) and the lazy-load-refreshed lookup (~637-647) now share one new private Actions.narrowOrHealOrThrowWhenAmbiguous helper instead of duplicating the #4321 narrowing logic. Investigated whether this was a genuine architectural fork (per the issue's own flagged risk) by reading ShaftHealingProvider.resolve() end-to-end (shaft-heal/src/main/java/com/shaft/heal/internal/ShaftHealingProvider.java:62-131): it keys purely off locator+pageKey+context and re-derives candidates from the saved LocatorFingerprint via its own independent DOM search (CandidateExtractor.extract), NEVER consulting the original locator's live match count or the caller-supplied element list. So calling resolve() from the multi-match branch required zero shaft-heal changes -- not a fork, confirmed by reading the code, not inferred. Decision on the #4326 cross-link question (Actions.java ~529-536): a last-resort-narrowed single element that seeds heal history via observe() is treated as an equally valid 'this is the element the user meant' signal as any naturally-unique match -- no special-casing, since the provider only ever scores against the live fingerprint, never its provenance.