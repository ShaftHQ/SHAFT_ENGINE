For installer and Assistant UI work, use native IntelliJ controls and a 12px outer / 8px group / 4px compact spacing rhythm; expose one primary task at a time; keep runtime copy version-neutral unless real probe evidence requires a version; use restrained warm-neutral hierarchy rather than ornament; preserve accessibility; validate light, dark, and 360px narrow screenshots. Issue #4388 is the implementation specification.