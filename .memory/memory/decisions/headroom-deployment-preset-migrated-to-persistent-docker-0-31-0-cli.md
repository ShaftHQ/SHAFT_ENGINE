Replaces the schtasks-based persistent-task deployment. The persistent-task preset cannot be (re)installed from a non-elevated shell: schtasks /Create /SC ONSTART returns Access denied, and a failed 'headroom install apply' deletes the old scheduled tasks AND the deployment profile, leaving ~/.claude/settings.json pointing at a dead proxy. Re-installed with --preset persistent-docker --scope provider --providers manual --target claude --port 8787 --backend anthropic --mode token --no-telemetry. Verified: host port binding 127.0.0.1:8787 only (container-internal 0.0.0.0 is Docker port-mapping mechanics, unlike the insecure 2026-07-15 community container), restart=unless-stopped, --no-telemetry in the container cmdline, 'headroom install status' healthy, settings env preserved. Trade-offs: image ghcr.io/chopratejas/headroom:latest lags PyPI (0.27.0 vs 0.31.0 CLI) and the container RW-mounts ~/.claude,~/.codex,~/.gemini,~/.config/opencode,~/.headroom (same user-level access the native pip install already had). Update path: pip install -U headroom-ai (stop any running supervisor first; pip cannot replace a locked headroom.exe), docker pull + re-apply for the runtime. Note: the previous save of this memory tripped the secret scanner because its slug contained 'ta&#8203;sk-to' which matches the OpenAI sk- key regex; avoid 'task-<word>' in memory titles.