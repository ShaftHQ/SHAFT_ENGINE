The main thread owns all substantive implementation, integration, and final verification/QA sequentially with real checks; the Workflow tool and saved/named workflows are permanently forbidden (`.claude/workflows/` must never be recreated) after a multi-agent workflow run for issue #3370 produced duplicated/conflicting classes from parallel implementers, died twice, and left six stray worktrees (2026-07-07). That same incident's Haiku-implemented parallel tasks specifically produced two independent conflicting classes, so Haiku was retired that same day. On 2026-07-08 Haiku was reinstated, bounded: a Fable subagent (fallback Opus) may do read-only architecture/planning research whose output the main thread itself reviews and applies; a Haiku subagent may make clearly minor, low-risk mechanical edits under the main thread's direct direction and review -- never unsupervised or parallel. Canonical current wording lives in AGENTS.md "Agent Hierarchy"; this object is the supporting incident history so it isn't lost to a non-portable memory store. Superseded in part 2026-07-20 by decision.chaos-engine-hierarchy-supersedes-orchestrator-ban-and-haiku-first-tiers-2026-07-20: delegation now routes through the Chaos Engine hierarchy; the Workflow-tool ban persists unless the owner explicitly asks in-session. This object remains the #3370 incident history.