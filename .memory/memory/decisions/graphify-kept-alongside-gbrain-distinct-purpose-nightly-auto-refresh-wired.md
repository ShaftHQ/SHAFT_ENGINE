Evaluated whether gbrain fully replaces graphify (user question 2026-07-16). Verdict: NOT redundant. graphify provides a deterministic structural repository map (file/module relations, pre-search file selection) as a zero-contention local file cache (graphify-out/, gitignored, shared read-only across worktrees) that works offline without embeddings. gbrain cannot cover this today: its link/graph layer is empty (Links=0 for federated code brains; reconcile-links missing from the installed binary's command registry) and its PGLite store is single-writer (locked whenever a gbrain serve MCP session is open). Division of labor codified in .claude/skills/graphify/SKILL.md: graphify answers structure, gbrain query/code-def answers meaning. Freshness automated: the nightly gbrain-dream scheduled task (05:00) now also runs 'graphify .' from the SHAFT_ENGINE main checkout. Tool updated 0.9.12 -> 0.9.17 (pip package graphifyy; README pin was stale at 0.8.50, now 0.9.17). Guard R5 nudge (issue #3611) remains the always-used enforcement.