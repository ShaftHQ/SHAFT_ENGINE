PR #4232 fixed ToolApprovalPromptPanel.WrappingArgumentsArea (shaft-intellij/.../ToolApprovalPromptPanel.java:252-301): missing getMinimumSize() let an ancestor BoxLayout compress a JTextArea below its wrapped-content height. Issue #4234 pattern-matched the same shape onto AssistantTranscriptView.PreferredHeightRow (AssistantTranscriptView.java:2090-2099, also getMaximumSize()-only, no getMinimumSize()). Full search found: PreferredHeightRow is instantiated at exactly 3 sites (widgetRow():836, createTruncationDivider():907, fallbackMessage():946), all of which add ONLY to fallbackPanel; fallbackPanel is unconditionally wrapped in fallbackScrollPane (a JBScrollPane) in the AssistantTranscriptView constructor (:165-185, scrollpane built :178/:636, added via BorderLayout.CENTER :180) with no branch/flag that skips it. Both production embedding sites (ShaftFeaturePanel.java:138 outputCard in a CardLayout, ShaftAssistantPanel.java:502 transcript) embed the whole AssistantTranscriptView without reaching into its internals, so the scrollpane protection is structural, not caller-dependent. Mechanism: JViewport does not force a non-Scrollable JPanel view to viewport size, so fallbackPanel always gets its full preferred height, so its own BoxLayout.Y_AXIS never has less room than the sum of its rows' preferred heights -- the shrink-then-clip precondition WrappingArgumentsArea depended on (an ancestor BoxLayout with less space than the sum of preferred sizes) never occurs here. Closed #4234 with this evidence trail; no code change made. If PreferredHeightRow is ever reused in a new non-scrolling container, re-open and apply the same getMinimumSize() { return getPreferredSize(); } fix PR #4232 used.