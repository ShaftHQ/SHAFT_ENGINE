Supersedes the embed-coverage blocker in decision.gbrain-mcp-adoption-spike-3611-not-ready-to-replace-or-pair-with-memory-as-of-2026-07-16 (which found embedded_count 0/734, no PGLite job-queue worker, and could not fix it from inside a live MCP session). Root fix applied outside any MCP session: set ~/.gbrain/config.json embedding_model to ollama:nomic-embed-text (768 dims) so `gbrain sync --strategy <markdown|code> --full` embeds inline during import instead of relying on the Postgres-only jobs-work queue that PGLite can never drain. Ran full syncs for shaft-engine: markdown strategy completed cleanly (295 files, 902 chunks, fully embedded). Code strategy (1660 files) was first killed at 47% (790 files) by the sync CLI's own default 1-hour hard-deadline watchdog (not a real error, exit 1 from SIGTERM) -- see the paired gotcha memory -- then resumed cleanly via the sync's own file-level checkpoint with --no-hard-deadline. Result as of this session: 1085 pages / 18079 chunks / 17705 embedded (97.9% coverage), climbing further as the resumed code sync finishes in the background. Verified real semantic search quality with a query for how ShaftEngine handles element healing: top hits scored 0.86-0.92 and were all genuinely on-topic (HealingActionsIntegrationTest.java, HealingStrategyTest.java, HealingSupport.java, VisualEvidenceService.java, HealingDecisionEngine.java), a sharp contrast to the prior spike's weak_semantic ~0.396 scores under empty embeddings. Compared to an equivalent grep -i heal across the repo (236 files, unranked, heavy false-positive rate e.g. matches on health/Healenium/healthcheck-unrelated files), gbrain's ranked top handful were all precise. Caveat: link_coverage is still 0 (Links stat) so gbrain's knowledge-graph/relation features remain unproven -- this decision only lifts the embedding/semantic-search blocker, not the broader authoritative-graph bar the original spike set. Do not migrate .memory/ content into gbrain; use gbrain search as a supplementary semantic-retrieval aid alongside grep/.memory, not a replacement.