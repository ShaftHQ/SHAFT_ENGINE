A standard root manifest alone is insufficient for the locally supported clients. Claude requires .claude-plugin/plugin.json; Codex requires .agents/plugins/marketplace.json plus .codex-plugin/plugin.json with the existing skills directory. The root Agent Plugins manifest remains authoritative and carries release metadata. Claude strict validation passed, while its live session was unavailable without host authentication. Codex discovered, installed, enabled, and read-only loaded the same fresh artifact, then its temporary marketplace and install were removed.