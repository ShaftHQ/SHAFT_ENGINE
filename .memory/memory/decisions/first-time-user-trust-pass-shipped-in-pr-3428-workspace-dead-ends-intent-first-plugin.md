PR #3428 (branch codex/first-time-user-e2e-trust) shipped the first-time-user trust pass, validated live from an external consumer project (C:/Users/Mohab/IdeaProjects/shaft-first-time-user) via the real installer plus stdio JSON-RPC runs. Durable decisions: (1) The MCP workspace root resolves from the client launch directory; the installer argfile writes ONLY -Dshaft.mcp.fallbackWorkspaceRoot - never -Duser.dir or -Dshaft.mcp.workspaceRoot; validate_shaft_mcp_configuration.py and verify_shaft_mcp_installer_release.py enforce this, and the bare user home never becomes a workspace. (2) Codegen tools generate from persisted recording JSONs with no live session; capture_status NOT_RUNNING carries recovery guidance plus the most recent recordings; doctor_analyze_failed_allure auto-discovers the newest populated allure-results (McpAllureResultsLocator) when allureResultPaths is empty. (3) The CaptureGenerator privacy gate compares personal paths against the common ancestor of the recording and the output directory (privacyAllowedRoot), never widening to the user home or above. (4) The IntelliJ plugin is intent-first: slash-command UI fully removed (typed slash still routes for compat), Assistant is the only non-expert view (workflow selector hides with one entry), defaults are Agent mode plus Allow source edits checked (mode switches no longer clear it), and the Why SHAFT block is replaced by a bottom-corner User Guide ActionLink. Docs PR shafthq.github.io#734 updated the intellij/mcp/installation guides.