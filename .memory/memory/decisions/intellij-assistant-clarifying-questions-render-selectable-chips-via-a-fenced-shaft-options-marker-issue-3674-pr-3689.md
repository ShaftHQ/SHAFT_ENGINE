Neither Claude's nor Codex's stream-json protocol carries a structured question/options shape -- confirmed by reading AssistantLocalAgentRunner's StructuredStreamParser end to end: the terminal event (Claude's 'result', Codex's 'turn.completed') only ever yields a plain answer string, never a question/options field. Detection is therefore an explicit protocol addition, not prose-pattern guessing: a trailing fenced ```shaft-options block containing a JSON array of 2-6 short option labels. New files: AssistantQuestion.java (record + detect(markdown) -> AssistantQuestion|null, strips the fence from the returned promptMarkdown), AssistantQuestionOptionsPanel.java (Swing chip row mirroring ToolApprovalPromptPanel's style; a chip click is non-destructive -- never disables, unlike a one-shot approval decision). Wired into ShaftAssistantPanel's two terminal-answer choke points, showResponse (cloud/tool-card answers) and finishLocalAgentResponse (streaming local-agent answers): the question is detected AFTER computing displayResponse, the STRIPPED markdown is what gets persisted via append()/replaceLastTranscriptAndChatState(), and only THEN is the chip widget shown via transcript.showWidget() -- order matters because append() clears any showing widget first (see AssistantTranscriptView.canAppendIncrementally's javadoc), so showing the widget before the persisted append would immediately wipe it. Clicking a chip fills the composer (ShaftAssistantPanel.fillPromptWithSuggestedAnswer) instead of auto-sending, matching the pre-existing emptyStateChip 'review and send it yourself' convention -- the always-available free-text fallback is untouched by design. The system-prompt instruction to actually emit the fence was added to AssistantCommand.SHAFT_MCP_USAGE_HINT, which flows into local-agent prompts (localAgentPrompt) only -- NOT into cloud-provider chat prompts (cloudPrompt's non-codegen branch), deliberately scoped out due to many exact-match (assertEquals on full text blocks) prompt tests in AssistantCommandTest.java; filed as follow-up issue #3690.