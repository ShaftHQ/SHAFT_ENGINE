SHAFT MCP web GUI automation keeps WebDriver as the default tool/codegen backend (`browser_*`, `element_*`, `capture_*`, `doctor_*`, `healer_run_failed_test`). Use `playwright_*` tools and generated `SHAFT.GUI.Playwright` code only when the target repository already uses SHAFT Playwright or the user explicitly asks for Playwright. Capture sessions can generate either backend; Appium/mobile and REST Assured scopes stay native and unchanged.