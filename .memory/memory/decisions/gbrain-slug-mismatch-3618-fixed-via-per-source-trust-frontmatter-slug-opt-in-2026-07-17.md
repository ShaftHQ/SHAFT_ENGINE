Resolves gotcha.gbrain-slug-mismatch-blocks-83-shaft-userguide-pages-extract-needs-source-db-on-db-only-brains. Implemented option 1 from #3618 (upstream gbrain feature, preferred) in the local checkout C:/Users/Mohab/gbrain, branch feature/trust-frontmatter-slug: new per-source config flag config.trust_frontmatter_slug (same JSONB shape as `federated`), CLI `gbrain sources trust-frontmatter-slug <id>` / `untrust-frontmatter-slug <id>`. importFromFile's anti-spoof SLUG_MISMATCH rejection honors the frontmatter slug for opted-in sources instead of rejecting it; threaded through sync.ts (incremental + --full) and import.ts's runImport (the actual per-file path performFullSync uses). Live-testing against the real 189-file shafthq.github.io source surfaced two real bugs fixed along the way: (1) Docusaurus root-relative slugs (`slug: /testing/web`) tripped validateSlug's leading-`/` security guard -- normalized (stripped) only on this explicitly-trusted path, guard stays strict elsewhere; (2) latent case-sensitivity bug -- putPage silently lowercases before storing but sibling calls in the same import (addTag) didn't, so `slug: bingAI` stored as 'bingai' but addTag('bingAI', ...) failed 'page not found'; fixed by lowercasing resolvedSlug once after either frontmatter-slug fallback resolves (also fixes the same latent bug in the pre-existing CJK/empty-path-slug fallback). Applied to the real shaft-userguide source and verified: 103 -> 183 pages after full resync (83 previously-blocked files now index; one file still skips on an unrelated Ollama embedding timeout). Upstream PR (unmerged, no push access to garrytan/gbrain): https://github.com/garrytan/gbrain/pull/2899 from MohabMohie/gbrain. Docs caveat updated: https://github.com/ShaftHQ/shafthq.github.io/pull/814. SHAFT_ENGINE#3618 closed. The fix is already live against the local checkout gbrain actually runs from (C:/Users/Mohab/gbrain), independent of upstream merge status.