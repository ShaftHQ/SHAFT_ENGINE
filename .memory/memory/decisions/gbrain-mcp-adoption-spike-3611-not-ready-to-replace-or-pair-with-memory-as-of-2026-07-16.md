Ran the real write/read round-trip spike suggested in #3611 (put_page -> get_page/search/recall) rather than just reasoning about it. Findings: (1) put_page's write_through side effect wrote a real markdown file directly into the CWD/project directory (not just gbrain's remote store) -- an untracked file appeared at repo root and had to be removed before commit; anyone invoking put_page from inside a repo working directory needs to check git status afterward. (2) get_page confirmed the write round-tripped correctly (content hash matched), but mcp__gbrain__search failed to find the just-written page for two of three query phrasings, and the one hit that succeeded scored only 0.396 (weak_semantic) vs about 1.0 or higher for pre-existing indexed pages -- consistent with get_health reporting embed_coverage 0, missing_embeddings 732, link_coverage 0 despite 223 already-ingested SHAFT_ENGINE pages (someone or something is already syncing repo docs into gbrain independent of this session). (3) mcp__gbrain__whoami failed outright with an unknown_transport server bug (remote call site must populate ctx.auth) over this remote MCP transport. Conclusion: gbrain's embedding/linking pipeline is not actually running in this environment today, so it behaves like an inconsistent keyword index, not the knowledge-graph value proposition number 3606 speculated about -- do not migrate .memory content or treat gbrain as authoritative until embed_coverage and link_coverage are non-zero and the whoami bug is fixed. Test page was soft-deleted via delete_page after the spike.