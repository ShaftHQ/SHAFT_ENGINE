Re-ran the gbrain live spike (issue #3708 scope) with the mcp__gbrain__* tools rather than re-reading the 2026-07-16 record. State changed dramatically. get_health embed_coverage 0.9895 (embedded_count 26216/26494 chunks) vs 0/734 before; the old root cause (PGLite queue cannot drain so embeddings never generate) is resolved -- doctor reports federation_health '3 sources healthy (parallel sync, async embed)', i.e. the bulk embedded out-of-band via host gbrain sync. Retrieval is empirically reliable and high quality: mcp__gbrain__query with source_id shaft-engine returns 0.87-0.91 high_vector_match hits across 5 varied phrasings (exact AllureManager.resolveAllureCommandPrefix, teardown quit/remove, the validator test + boundary-guard skill). WITHOUT an explicit source_id, query/search/list_pages/get_page all return [] because the remote transport defaults to the empty default source and __all__ spans only granted sources, and the whoami unknown_transport bug leaves remote callers with no default grant; naming source_id works. Reranker stays degraded (reranker_health WARN, 2 ZEROENTROPY auth failures, no key coming) but vector-only retrieval is strong without it. brain_score still 45/100 but now driven by link_coverage 0 (link_count 2, 1619 orphan_pages, 246 bare wikilinks with no basename match), timeline_coverage 0, and 278 residual missing embeddings -- NOT the embed collapse of 2026-07-16; residual embed-backfill job 35 still sits waiting (attempts_started 0), confirming the MCP-session queue still never drains, but that is 1.1 percent and immaterial to retrieval. Verdict per the user instruction 'if gbrain works and is reliable, keep it': KEEP gbrain as a fully-local retrieval accelerator, always passing source_id shaft-engine; .memory/ plain markdown + wikilinks stays the canonical source-controllable store, which already satisfies both user hard requirements (fully local, git-trackable). gbrain DB/graph/embeddings are a derived local index, NOT source-controllable, so gbrain supplements, never replaces .memory. No Obsidian migration needed. The put_page write-through leak (drops a file into the gbrain server CWD = the main SHAFT_ENGINE repo, where the release PR work runs) was deliberately left UNVERIFIED this session to avoid colliding with that work.