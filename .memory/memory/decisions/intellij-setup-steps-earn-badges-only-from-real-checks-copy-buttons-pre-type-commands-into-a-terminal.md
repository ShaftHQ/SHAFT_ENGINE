PR #3427 (issue #3426): setup-step badges derive ONLY from real verification - ShaftProjectVersionCheck compares the project pom SHAFT version (with one ${property} indirection) against SetupPrerequisites.knownLatestEngineVersion(); agent step needs actual readiness detection; install step needs inferInstalledStdioCommand() to find shaft-mcp on disk; the Check-setup probe gets an explicit red 'failed' badge state. Click-tracking booleans (installerCommandCopied/terminalOpened/upgradeCommandCopied) were removed. Copy-command buttons open an IntelliJ terminal with the command PRE-TYPED via ShaftTerminalCommands: optional org.jetbrains.plugins.terminal plugin dependency, reflection over createLocalShellWidget + getTtyConnector().write(command) (no newline - the user presses Enter), newer createShellWidget/getTtyConnectorAccessor as fallback, clipboard fallback when the terminal plugin is absent. The engine warm-up command must keep its -Dartifact argument double-quoted: PowerShell splits an unquoted -Dartifact=io.github... token at the first dot.