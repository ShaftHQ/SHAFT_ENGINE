PR #3427 (issue #3426): (1) clicks on invisible targets are browser-synthesized (Enter-in-form 'clicks' the hidden default submit button) and are suppressed in shaft-capture-recorder.js AND CaptureEventPipeline.emitClick (visible==false); (2) a committed change event re-announcing the last recorded input value is forwarded so pending inputs still flush, but never appended as a new step (client lastInputEmissions map + pipeline lastEmittedInputValues map); (3) body/html clicks and page clicks while the assertion wizard panel is open are never recorded; (4) assertion element-pick mode shows a top-center banner + crosshair cursor + hover highlight, Esc cancels. (5) /upgrade routes to the local agent (AGENT mode + allowSourceMutation gate in AssistantCommand.upgradeAgentGate) with a --yes non-interactive upgrader command because local agent CLIs close stdin immediately; cloud/non-CLI routes keep the manual command. (6) Verbose applies to every route: MCP tool runs echo the exact request JSON and raw response; capture_generate_replay renders a full generate/compile/replay story via AssistantMarkdown.captureReplayMarkdown plus an up-front ShaftAssistantPanel.toolRunNarration bubble, never a bare Done.