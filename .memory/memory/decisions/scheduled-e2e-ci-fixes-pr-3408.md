Root-caused and fixed the 2026-07-08 scheduled CI failures (runs 28916954272 "E2E Tests" and 28917051358 "Local E2E Tests") via PR #3408:

1) shaft-sikulix/pom.xml excluded org.openpnp:opencv from sikulixapi's transitive deps without ever re-adding it directly (unlike shaft-visual, which needs the same capability and does declare it) -- Windows_SikuliX_Local had failed 8/8 runs since the module existed, with NoClassDefFoundError: org/opencv/core/Core. Fixed by adding org.openpnp:opencv:4.9.0-0 as a direct dependency, matching shaft-visual/pom.xml.
2) GUIWizardTests.test_selectOptionNotExistFromDropdownList expected AssertionError, but Actions.report() in shaft-engine's Actions.java always wraps action failures in a plain RuntimeException (AssertionError is only thrown by the separate validation/accessibility APIs). git log --follow -p confirmed this expectation was wrong since the test's original commit, not a regression. Fixed by correcting expectedExceptions to RuntimeException.class.
3) .github/workflows/e2eLocalTests.yml's "appium driver run windows install-wad" step launches the WinAppDriver MSI via an unawaited elevated Start-Process and reports success ~0.3s later (confirmed exact timing from the run log), racing the next step -- Windows_Appium_Desktop_Local failed 8/8 runs with a 300s session-create timeout. Fixed by adding a readiness poll for WinAppDriver.exe before starting Appium, and capturing the Appium server's own log during shutdown for future diagnosis.

Already explained, no code change: ultimateqa.com/complicated-page contact-form NoSuchElementException across Android/iOS/Safari BrowserStack + Ubuntu_JUnit_E2E was a same-day third-party site incident (element confirmed present on the live page again via curl; locator passed 6 of the last 7 scheduled runs). The axe.min.js-not-on-classpath AccessibilityTest cascade in Windows_Chrome_Local/Windows_Edge_Local predates PR #3383's already-merged pom.xml resource-include fix.

Filed issue #3407 separately for an unrelated OutOfMemoryError in AllureManager.patchGeneratedAllureReportIndex (Files.readString + substring-based copies of the whole single-file Allure report) found incidentally -- MacOSX_Safari_Local/MacOSX_Chrome_Local crashed in report-patching teardown after all tests had already passed, so the job stayed green.