Re-verifies and root-causes the Links=0 limitation noted in decision.graphify-kept-alongside-gbrain-distinct-purpose-nightly-auto-refresh-wired (2026-07-16). Live-checked while confirming shaft-engine was fully parsed+linked as a #3618 follow-up: shaft-engine is fully and currently synced (1450 pages, HEAD-current -- was ~7h stale, resynced) and code-def/code-refs symbol lookup works correctly, but the link/call graph is genuinely empty: `gbrain extract links --source db --source-id shaft-engine` walked all 1450 pages and created 0 links; `code-callers`/`code-callees` report status:'indexing', ready:false. Root cause found (not previously known): `reconcile-links` -- the command that builds this graph -- has a full case handler in gbrain's src/cli.ts handleCliOnly switch (~line 1694) but 'reconcile-links' was never added to the CLI_ONLY allowlist Set literal (~line 47), so every invocation falls through to the generic 'Unknown command' branch before reaching its own handler. A one-line registration gap, not a deep bug. Filed upstream: https://github.com/garrytan/gbrain/issues/2900 (no code fix attempted this session, by user direction -- report-only). Consequence: skills/retrieval-reflex/SKILL.md and .claude/skills/graphify/SKILL.md both updated 2026-07-17 to stop directing agents to gbrain's graph/backlink/code-caller/code-callee tools for structural questions until gbrain#2900 lands -- use graphify for those instead. Re-check both skills once the upstream fix merges.