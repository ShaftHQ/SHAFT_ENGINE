PR #4031 reconciled locator guidance across `shaft-skills/` and `.agents/skills/act-as-mohab/references/shaft-mastery/locator-healing.md`: generated code prefers `SHAFT.GUI.Locator.hasRole(...)`; native `By.xpath(...)` is the fallback when no ARIA role exists; Smart Locator is excluded from generated/codegen output and is only for temporary human exploration. The SMART_LOCATOR diagnostic remains WARNING pending #4028.