PR #4185 (closes #4172, follow-up to #4161/PR #4173, subtask of tracker #4024) wires CaptureGenerator.generate() to call HealingManager.observeFingerprint(...) for every target right after a PASSED compile+replay, mapping ElementSnapshot/LocatorCandidate onto HealingFingerprintSeed and building the exact runtime By via new CaptureGenerator.runtimeLocator/runtimeSemanticLocator (mirrors locatorExpression/semanticLocator branch-for-branch, real Locator/By objects instead of source strings). The generated test's setUp() now emits an ABSOLUTE healing.history.path (outputRoot/.shaft-heal/history.json, never target/), healing.strategy=shaft-heal, and healing.ai.trigger=below-threshold. Two decisions made deliberately rather than inherited: (1) seeding stays ungated on healing.strategy (matches recordOutcome()'s existing precedent from PR #4173, not resolve()'s gated one) -- accepted consequence: seeding fires even under healing.strategy=disabled, populating history a gated resolve() won't read until healing is later enabled. (2) the generated setUp()'s healing override is emitted unconditionally regardless of maximumPerformanceMode, treated as an accepted intentional side effect either way it resolves against the TestNGListener.onStart(ISuite)-before-@BeforeMethod ordering (traced-but-unproven contract per the PR #4173 corrected memory record) -- since the design does not depend on which way the ordering resolves, no new TestNG-suite-runner integration test was added to settle it. New test CaptureGeneratorHealingSeedingTest (shaft-capture) proves the acceptance criterion end-to-end, following ShaftHealingProviderTest#seededFingerprintShouldSatisfyHistoryLookupWithoutALiveElementOrDriver's shape one layer up. shaft-capture's pom.xml gained shaft-heal as a TEST-ONLY dependency (production code still has zero compile-time dependency on shaft-heal). Known risk: external PR #4181 (open, unmerged as of #4185, fixes issue #4166) touches the same CaptureGenerator.generate() replay region (moves atomicWrite calls after compile/replay, gated on a new 'successful' boolean) -- if #4181 merges first, expect a textual merge conflict requiring a rebase of the seedHealingHistory(...) call site.