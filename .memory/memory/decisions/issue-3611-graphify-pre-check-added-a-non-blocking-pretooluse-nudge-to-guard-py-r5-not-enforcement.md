Extended .claude/hooks/guard.py PreToolUse matcher in settings.json from Bash|PowerShell to Bash|PowerShell|Read|Grep. After 6 broad Read/Grep calls in one session with no evidence graphify was consulted (a resolve_graph_out.py --check Bash call, or a Read under graphify-out/), the hook emits ONE PreToolUse response with permissionDecision allow plus additionalContext -- verified against current Claude Code hooks docs (via claude-code-guide agent) that this is a real, documented non-blocking mechanism distinct from deny. State is a best-effort per-session JSON file under the OS temp dir (overridable via SHAFT_GUARD_STATE_DIR for the self-test), fails open on any read or write error. Deliberately never denies or blocks -- issue 3611 explicitly asked for a narrow low-risk signal, not enforcement, since a wrong heuristic on a hook that gates every tool call for every contributor would be actively harmful if it blocked legitimate targeted lookups. Covered by a new self-test function run_graphify_self_test exercising: below-threshold silence, threshold nudge fires once, no repeat nudge, suppression via resolve_graph_out.py bash call, suppression via graphify-out Read, and fail-open on missing session_id -- all passing alongside the existing 54 R1-R3 cases, 64 total passing.