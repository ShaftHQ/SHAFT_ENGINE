2026-07-27, owner-directed (#4260, PR #4261). The charter previously lived in three places simultaneously — .claude/agents/chaos-engine.md, AGENTS.md 'Agent Hierarchy & Model Routing', and the skill's ## Delegation section — the same divergence shape that produced the F1/F9 prompt bug in #4239.
Resolution: .claude/skills/act-as-fable renamed to .claude/skills/act-as-mohab (trigger phrase 'act as mohab'); the charter folded in as a new '## Assume the orchestrator role' section plus references/orchestrator-bootstrap.md carrying the bootstrap mechanics (scope repos, gh issue/pr/worktree/run state, priority order in-progress > open PRs > bugs > red main > direct asks > backlog, file-scope overlap check before parallelizing, 2-4 concurrent with hard cap 4, ticket before dispatch, bounded specs only, gh issue create for adjacent findings, covenant + 'act as mohab' + TDD in every dispatch prompt). .claude/agents/chaos-engine.md reduced to a thin pointer restating zero policy.
CRITICAL distinction preserved: the Claude MODEL named Fable ('Fable at high effort', 'Opus/Fable') is NOT the skill and was deliberately not renamed — conflating them would corrupt model/effort routing.
Bulk content went to references/ because .claude/skills/*/SKILL.md is capped at 16384 LF bytes; no budget in agent_guidance_budget.json was raised.