Issue #3934 (re-validate LambdaTest via workflow_dispatch and re-enable the nightly cron) is closed as wontfix, not resolved. The workflow_dispatch re-validation run (https://github.com/ShaftHQ/SHAFT_ENGINE/actions/runs/29828239543, 2026-07-21) reproduced the identical failure documented since #2927: LambdaTest_NativeAndroid fails with `org.openqa.selenium.SessionNotCreatedException: ... Response code 403 ... LT_FEATURE_NOT_AVAILABLE_IN_CURRENT_PLAN`. No code-side change can start a session under the current plan. Why: the repo owner has deliberately decided to limit LambdaTest license/plan spend going forward -- upgrading the account plan (the only real fix) is out of scope by business choice, not a technical blocker anyone should keep chasing. How to apply: the nightly `schedule:` cron in `.github/workflows/lambdatestTests.yml` stays disabled (dispatch-only), as set by #3473. Do NOT reopen or attempt to fix #3934, #2927, or the LambdaTest plan limitation without new, explicit owner direction (e.g. an owner decision to upgrade the plan, pursue an OSS license grant, or otherwise change the LambdaTest account). Treat any future 403 LT_FEATURE_NOT_AVAILABLE_IN_CURRENT_PLAN failure on LambdaTest jobs as this same known, accepted constraint, not a new bug.