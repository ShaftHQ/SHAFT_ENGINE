2026-07-17 scan of shaft-intellij filed a second UX program (distinct from the closed #3553 program). Three confirmed gaps drove it: (1) browser/headless are custom.properties-only, no per-run IDE control -- ShaftTestNgRunConfigurationProducer/ShaftJUnitRunConfigurationProducer only pick class/method, no VM args; (2) RecordShaftFlowHereAction only prefills a request, doesn't launch a live recording, and three inconsistent insert-at-X mechanisms exist (RecordShaftFlowHereAction insertAfter, ShaftAssistantPanel 'Insert into open class', GuidedWorkflowPanel 'Insert at caret'); (3) ShaftTestsPanel is a flat post-run history list (ShaftTestIndex: 'v1 granularity: one row per run-configuration name, not per @Test method'), not a real discovery tree with live status. Children: #3659 run-config SHAFT tab (browser+headless per-run overrides), #3660 recorder browser picker + un-gate headless checkbox (GuidedWorkflowPanel hardcodes 'Chrome' at line 635), #3661 true live record-at-cursor, #3662 unify the three insert mechanisms, #3663 static test discovery tree (prereq for #3664), #3664 live run status + per-node run/debug via SM test-runner protocol, #3665 curate ShaftSettingsConfigurable + Recorder tab (replace ShaftFeaturePanel raw-JSON form).