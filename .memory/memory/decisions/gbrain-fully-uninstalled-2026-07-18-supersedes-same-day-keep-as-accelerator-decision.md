User explicitly requested a full wipe/uninstall of gbrain on 2026-07-18, hours after the 'keep gbrain as local accelerator' re-spike decision from the same day. Executed: removed gbrain-ollama Docker container + gbrain_ollama_data volume; deleted gbrain-autosync and gbrain-dream Windows scheduled tasks; deleted ~/.gbrain (PGLite data/logs) and ~/gbrain (git checkout) and ~/.bun/bin/gbrain(.exe/.bunx). Repo updated to stop expecting gbrain: AGENTS.md, .claude/skills/act-as-fable/SKILL.md, .claude/skills/graphify/SKILL.md, CONTRIBUTING.md all had gbrain references removed; skills/retrieval-reflex/ deleted entirely (its sole purpose was gbrain retrieval, it was itself gbrain-installed via .gbrain-source.json). graphify's nightly auto-refresh, previously piggybacked on the gbrain-dream scheduled task, was preserved by extracting it into tools/agent-infra/graphify-refresh.cmd with its own graphify-refresh scheduled task (daily 05:00) -- graphify itself was explicitly kept, only gbrain was removed. Spike verdict retained for the record: the #3611 adoption spike (2026-07-16) found gbrain not ready to replace or pair with memory (search/recall missed just-written pages; put_page wrote untracked files into the repo CWD), and the same-day 2026-07-18 'keep as accelerator' re-spike was overridden by this uninstall hours later. Historical gbrain operational entries (adoption spike, embed-coverage blocker, link-graph bug, slug-mismatch fix, brain-score breakdown, Windows schtasks setup, use-gbrain-advisor constraint, keep-as-accelerator re-spike) were deleted in the 2026-07-28 memory-hygiene pass -- the loader cannot down-weight dead entries (issue #4287), and git history preserves them. decision.obsidian-migration-rejected-memory-already-meets-local-source-controllable-gbrain-accelerator-recovered and decision.graphify-kept-alongside-gbrain-distinct-purpose-nightly-auto-refresh-wired stay active as durable abandoned-attempt/architecture records.