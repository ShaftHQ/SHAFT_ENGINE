P0.3 (finding F7): CaptureService.generateReplay (the @McpTool-scanned capture_generate_replay overload, CaptureService.java ~line 975) had NOT ONLY the reported `replay` boolean missing @McpToolParam -- outputDirectory, packageName, className, overwrite, useAi, allowLocalAi, allowRemoteAi, and driverVariableName were ALL unannotated too, meaning the live schema silently marked all 9 as required with null descriptions (confirmed via the live-served schema in ToolIndexMechanicalDumpTest's dump: required flipped true->false for every one once @McpToolParam(required=false, description=...) was added). replay's description states the real, verified behavior read from CaptureGenerator.java (~line 363-369)/CaptureGenerationReport.Status: replay=true is required for report status SUCCESS (an actual replay pass); replay=false, or any replay that never runs (e.g. after a compile failure), yields codegen-only output marked UNCONFIRMED, never SUCCESS. P0.4 (finding F6): capture_start dispatches purely on EngineService.activeEngine(), so mobile capture without a prior driver_initialize silently records a desktop Chrome browser. Investigated mirroring capture_api_start's mobilePlatform-override pattern (CaptureService.java ~line 530-543) but it doesn't transfer: capture_api_start's mobile path is a standalone MITM loopback proxy needing no live driver, while capture_start's mobile branch (MobileService#recordStart) records off an ALREADY-ACTIVE driver -- launching one from inside capture_start would need the full driver_initialize/EngineService#registerMobileInitBridge session-lifecycle machinery (Appium session creation), which is architectural surgery, not a small parameter bolt-on. Took the documented fallback: strengthened capture_start's @Tool description to loudly state the desktop-browser default and the required driver_initialize(engine=mobile_native|mobile_web) prerequisite for mobile. Both combined into PR #4243 since they touch the same file.