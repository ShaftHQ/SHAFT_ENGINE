For changes to `FailureTraceReporter.renderTraceHtml` or any SHAFT embedded single-file HTML viewer (unit tests only assert string markers; a JS syntax/runtime error ships silently, and the in-app Browser refuses file:// URLs): (1) write a temporary probe TestNG test in shaft-engine that records realistic actions/network/console via TraceEventRecorder/BrowserObservabilityRecorder, calls `FailureTraceReporter.attachOnFailure`, and extracts the HTML from the persisted `target/shaft-traces/<id>/shaft-trace.zip` (delete the probe before committing); (2) smoke-execute the extracted `<script>` in Node with a ~30-line DOM stub against the real embedded trace JSON, appending calls to every `renderTab(...)` path (catches syntax + runtime errors); (3) screenshot with headless Chrome CLI (allowed, not a GUI-open): `chrome.exe --headless=new --disable-gpu --window-size=1440,1000 --screenshot=<abs.png> file:///<abs>.html`, one invocation at a time (parallel/chained calls silently produce nothing) -- since content is JS-rendered, a populated screenshot proves execution; (4) commit PNGs under `.github/pr-evidence/<topic>/` and hotlink raw URLs. Gotchas found this way: Java text-block `\n` inside embedded JS must be written `\\n`; viewer CSS classes collide with generic badge classes (prefix them, e.g. `kind-action`); `TraceEventRecorder.callerFrame` filters `com.shaft.*` so SHAFT's own unit tests get a blank caller (assert key presence only).