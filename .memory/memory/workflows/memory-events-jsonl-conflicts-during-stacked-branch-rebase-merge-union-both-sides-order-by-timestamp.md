Confirms and reinforces the existing gotcha.memory-events-jsonl-append-log-conflicts-on-every-concurrent-session-merge: during the PR #3647/#3648 rebase-and-merge chain this session, resolved 6+ separate events.jsonl conflicts this way with zero issues -- always keep both sides' appended lines (never drop either), interleaved by each line's timestamp field. This is mechanical and safe to repeat every time two sessions' branches both touched memory in the same window.