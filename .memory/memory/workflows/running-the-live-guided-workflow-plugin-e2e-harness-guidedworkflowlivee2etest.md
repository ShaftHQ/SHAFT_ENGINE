GuidedWorkflowLiveE2ETest drives web recorder->codegen, mobile recorder->codegen, and doctor_analyze_failed_allure through real GuidedWorkflowPanel clicks plus ShaftMcpStdioClient against a real SHAFT MCP server. Use JDK 25 and the checked-in wrapper: `shaft-intellij/gradlew[.bat] -p shaft-intellij test --tests com.shaft.intellij.ui.GuidedWorkflowLiveE2ETest -Dshaft.intellij.liveWorkflows=true -Dshaft.intellij.liveMcpCommand='<java> @<shaft-mcp.args>' -Dshaft.intellij.workspaceRoot=<scratch-dir>`. The installed launcher's args classpath points at the local Maven repository, so locally patched modules require a deliberate `mvn -pl <module> install -DskipTests -Dgpg.skip`. A self-driving web fixture must dispatch a committed `change` event after `input`, or values can remain pending and never increment capture_status eventCount.