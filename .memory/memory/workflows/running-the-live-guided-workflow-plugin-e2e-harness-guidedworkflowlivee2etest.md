GuidedWorkflowLiveE2ETest (shaft-intellij) drives web recorder->codegen, mobile recorder->codegen, and doctor_analyze_failed_allure through real GuidedWorkflowPanel clicks plus the plugin's ShaftMcpStdioClient against a real SHAFT MCP server. Command shape: JAVA_HOME=<jdk21> gradle -p shaft-intellij test --tests com.shaft.intellij.ui.GuidedWorkflowLiveE2ETest -Dshaft.intellij.liveWorkflows=true -Dshaft.intellij.liveMcpCommand='<java> @C:/Users/Mohab/AppData/Local/ShaftHQ/shaft-mcp/versions/<version>/shaft-mcp.args' -Dshaft.intellij.workspaceRoot=<scratch dir, e.g. shaft-intellij/build/live-workflows>. The installed launcher's args classpath points at ~/.m2, so to live-test locally patched modules run mvn -pl <module> install -DskipTests -Dgpg.skip first (deliberate local-shadow; jars under the release version coordinate stay overwritten afterwards). Fixture note: a self-driving web fixture must dispatch a committed 'change' event after 'input' (as real browsers do on blur) or typed values park in CaptureEventPipeline.pendingInputs and never appear in capture_status eventCount.