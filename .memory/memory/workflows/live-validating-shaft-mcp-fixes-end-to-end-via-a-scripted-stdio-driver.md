Install fixed modules with mvn -pl shaft-engine,shaft-capture,shaft-mcp install -DskipTests -Dgpg.skip=true (release gpg:sign fails locally without -Dgpg.skip). Copy the installed ShaftHQ argfile (AppData/Local/ShaftHQ/shaft-mcp/versions/<v>/shaft-mcp.args) swapping its FIRST -cp entry to the freshly installed .m2 shaft-mcp jar, then drive newline-delimited JSON-RPC over stdio from a python script (initialize, notifications/initialized, tools/call) with cwd set to a scratch workspace containing a local sample.html. Gotcha: capture_start_codegen requires the FULL request object -- the generated JSON schema marks every field of the request POJO required. This validated the #3429 fixes: bridge-driven element_type recorded events, an externally deleted session file self-healed, capture_stop COMPLETED, code generated, and a second session started with no wedge.