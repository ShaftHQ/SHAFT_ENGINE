Owner directive (2026-07-20): every new substantial work request is analyzed/planned/architected first, then tracked via ONE tracking issue titled `Tracking: ...` whose body holds a checkbox list, one box per subtask. Each subtask also gets its OWN real linked GitHub issue, not just a checkbox. Related subtasks are grouped into PRs -- one PR per group, not one PR per session -- and each PR body carries one `Closes #N` line per subtask issue it completes. When a subtask closes: check its box in the tracking issue and post a progress comment there. When the LAST subtask closes: close the tracking issue itself with a final summary comment. This supersedes the older '1 PR/session' default in AGENTS.md New Task Flow. Encoded in AGENTS.md New Task Flow and .claude/skills/work-github/references/playbook.md Sec. 3b (with gh issue create/edit/comment examples). Applies to every future session and every agent, not just the tool-architecture-sweep session (tracker #3866) it was first applied in.