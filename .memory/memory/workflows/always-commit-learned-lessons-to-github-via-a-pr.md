User standing preference (2026-07-14): learned lessons must ALWAYS be committed to GitHub, not left only in local memory. Workflow at Completion / Learning Loop: after recording durable lessons into the repo-canonical .memory/ store (via `memory remember --stdin`, which writes JSON+MD+index under .memory/memory/), commit the resulting .memory/ changes on the session branch, push, open a ready PR, and merge it. Note: the ~/.claude per-project auto-memory is NOT part of the SHAFT git repo and cannot be pushed to it; the git-tracked store is the repo's .memory/ directory, so durable lessons that must reach GitHub go through .memory/ + a PR.