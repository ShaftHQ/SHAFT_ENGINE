For new SHAFT_ENGINE tasks that require code or documentation edits, fetch the remote and start from a fresh dedicated branch based on `origin/main` before making changes, unless the user explicitly asks to continue an existing branch. Preserve unrelated user work in any dirty worktree by using a clean worktree or separate checkout when needed. Evidence: user explicitly instructed this on 2026-06-16.