For file-editing SHAFT_ENGINE tasks, preserve user work, fetch/prune, fast-forward local `main` from `origin/main`, create a fresh `codex/*` branch from `origin/main`, implement, run the smallest relevant validation, commit, push, open a ready PR, and report the PR link. Reuse that same branch/worktree for the whole session -- treat further sub-tasks as additional commits, not new branches (AGENTS.md New Task Flow). Use draft only when blocked, incomplete, or requested. If a PR closes issues, include each `Closes #...` line. PR bodies should include concrete examples for new public APIs or logging changes, and screenshots for UI/reporting changes when applicable. Do not wait for CI after opening or updating a PR unless the user explicitly asks.