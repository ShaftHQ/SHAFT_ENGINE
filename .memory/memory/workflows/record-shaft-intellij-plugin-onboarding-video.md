When asked to create a real SHAFT IntelliJ IDEA plugin video, follow tools/intellij-plugin-recording/README.md: build the latest local plugin ZIP, install it in an isolated IntelliJ sandbox, run first-run MCP setup with CODEX/CLI, test the connection, use Assistant AGENT mode with the prompt starting `open chrome and login to saucedemo`, require marker `SAUCEDEMO_LOGIN_DONE`, record MP4 H.264 30 fps full desktop without audio, upload the MP4 to Google Drive root, and link it in the PR body.