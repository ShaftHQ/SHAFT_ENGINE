When asked to create a real SHAFT IntelliJ IDEA plugin video, follow tools/intellij-plugin-recording/README.md: build the latest local plugin ZIP, install it in an isolated IntelliJ sandbox, select Family CODEX and Runtime CLI before clicking Install / Update SHAFT MCP, verify the install configures the plugin MCP command and the selected agent's shaft-mcp entry, test the connection, use Assistant AGENT mode with Allow edits and the prompt starting `open duckduckgo and search for SHAFT Engine`, ensure the browser window is in focus, require marker `DUCKDUCKGO_SHAFT_DONE`, and ensure normal local agent chat renders the selected agent's Markdown response directly without raw JSON, `autobot_local_agent_run`, or SHAFT status metadata. For branch recordings before merge, launch the IDE with `SHAFT_MCP_INSTALLER_REF` set to the current branch so first-run install uses the branch installer. Record MP4 H.264 30 fps full desktop without audio, upload the MP4 to Google Drive root, and link it in the PR body.