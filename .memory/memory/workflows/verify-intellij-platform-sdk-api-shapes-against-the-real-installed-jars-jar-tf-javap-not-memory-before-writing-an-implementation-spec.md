Before writing an IntelliJ Platform implementation spec, inspect the actual installed SDK jars using `jar tf <jar>` to locate classes and `javap -classpath <jar> <class>` to confirm packages and signatures. SDK APIs move across versions, so live evidence prevents plausible but wrong imports and overload assumptions. This previously confirmed that `VirtualFileWrapper` lives in `com.intellij.openapi.vfs` and that `FileSaverDialog` has a `save(java.nio.file.Path, String)` overload. Apply this as act-as-mohab live-probe-before-external-implementation guidance.