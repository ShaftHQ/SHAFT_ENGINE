When updating the official SHAFT user guide, reuse a permanent checkout under `C:\Users\Mohab\IdeaProjects` instead of rediscovering the repository each time. Use targeted `rg` searches and small file excerpts, avoid loading broad docs trees, and edit only the pages directly related to the functionality change. Prefer validating with the smallest docs checks first (`npm run test:docs`, then build only when link/MDX confidence is needed). Evidence: user explicitly instructed this on 2026-06-16.