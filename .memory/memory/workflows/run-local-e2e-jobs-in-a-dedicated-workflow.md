Local browser and local Cucumber GitHub Actions jobs live in `.github/workflows/e2eLocalTests.yml` with the same daily 01:00 UTC schedule and manual dispatch as the main E2E workflow. Keep provider/grid jobs in `e2eTests.yml` or their provider-specific workflow so local-runner issues can be reviewed independently. Manual run 27715249474 at PR head 940452f65b showed `Windows_Edge_Cucumber_Local` and `MacOSX_Chrome_Local` passing, while `Windows_Edge_Local`, `Windows_Chrome_Local`, and `MacOSX_Safari_Local` were still inside `Run tests` after an extended wait and had to be cancelled; future local workflow work should scope or shard those broad browser jobs and add explicit `timeout-minutes` so they cannot run indefinitely.