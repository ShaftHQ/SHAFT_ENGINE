Local browser and local Cucumber GitHub Actions jobs live in `.github/workflows/e2eLocalTests.yml` with the same daily 01:00 UTC schedule and manual dispatch as the main E2E workflow. Keep provider/grid jobs in `.github/workflows/e2eTests.yml` or provider-specific workflows so local-runner issues can be reviewed independently. Environment-independent unit tests belong to `pr-gate.yml`'s `unit-tests` job and must be excluded from both broad browser matrices; `scripts/ci/validate_quality_configuration.py` enforces exact shared scope, preserves grid-only Playwright/Lazy-loading differences, and binds ownership to the actual PR-gate job and step. This prevents the duplicated coverage-instrumented unit load that exhausted the MacOSX_Safari_Local Surefire heap in run 31454276391 (#4696).