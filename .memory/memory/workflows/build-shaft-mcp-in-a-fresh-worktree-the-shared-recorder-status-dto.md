Build/test: upstream reactor artifacts (shaft-engine, shaft-capture, shaft-doctor, shaft-ai, shaft-pilot-core, shaft-capture-proxy) are NOT in `.m2` for a fresh worktree, and `install` triggers gpg signing. Install the chain once before scoped tests: `mvn -pl shaft-mcp -am install -DskipTests -Dmaven.test.skip=true -Dgpg.skip=true -Dspotless.check.skip=true` (JAVA_HOME=JDK25); then scoped `mvn -pl shaft-mcp test -Dtest=<Class>` works (read surefire XML for the verdict). Shared DTO: `com.shaft.mcp.McpMobileRecordingStatus` is the recorder status DTO for BOTH the mobile recorder (`McpMobileRecordingService`/`MobileService`) AND the Playwright recorder (`McpPlaywrightRecordingService`/`PlaywrightService`) -- they also share `McpMobileRecording`/`McpMobileRecordedAction`. A DTO field addition needs a back-compat constructor (each service has ~4 construction sites) and benefits both recorders. `AssistantMarkdown` dispatch is structural, not tool-name based -- Playwright status renders through `mobileRecordingStatusMarkdown` because it shares the DTO shape. Readiness reuses `com.shaft.capture.model.CaptureReadiness.State` (do not fork a per-recorder readiness enum). Evidence when the in-app browser hangs: it times out on local file servers and file:// is blocked -- serve via `py -3 -m http.server` then headless Chrome to a user-writable dir (writing into temp/scratchpad gets Access denied); commit PR evidence PNGs under `.github/pr-evidence/<slug>/`.