Canonical user-level Claude adapter files live in `.claude/user-harness/`, while the substantive provider-agnostic entrypoint lives in `.agents/skills/act-as-mohab/`. Run `py -3 scripts/agents/sync_user_harness.py` to check drift or add `--apply` to deploy with `.bak` backups. `settings.json` is an owned-key recursive merge: tracked non-secret keys are updated while unrelated environment variables, plugin settings, credentials, and personal keys remain intact. Other managed files are exact copies. Secrets and runtime state remain outside the repository.