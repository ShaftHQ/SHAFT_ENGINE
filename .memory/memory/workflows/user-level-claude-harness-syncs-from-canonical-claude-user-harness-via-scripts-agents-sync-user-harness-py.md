Canonical copies of ~/.claude user-level files (CLAUDE.md shim, settings.json, statusline-command.sh) live in .claude/user-harness/. Run `py -3 scripts/agents/sync_user_harness.py` (--check default, exit 1 on drift; --apply deploys with .bak backups). Comparison is CRLF-normalized so autocrlf can't fake drift. The script never touches secrets (.credentials.json) or runtime state; secrets live only in ~/.claude and are never committed. The deployed ~/.claude/CLAUDE.md is a shim pointing non-SHAFT sessions at the repo's act-as-fable SKILL.md and graphify references/full-pipeline.md. User-level duplicate skills (act-as-fable, graphify) were deleted; repo copies are canonical (duplicate skill names at user+project level double-list in the harness skill roster and drift).