'gbrain autopilot --install' has NO Windows target (launchd/systemd/ephemeral-container/linux-cron only; bash wrapper). The deployed equivalent: two user-level Scheduled Tasks (no elevation needed, unlike ONSTART) registered by the source-controlled installer SHAFT_ENGINE/tools/agent-infra/install-agent-tasks.ps1 and pointing at the repo's tools/agent-infra/*.cmd scripts (logs stay machine-local in ~/.gbrain/autopilot/): gbrain-autosync every 30 min ('gbrain sync --all --no-pull --skip-failed --no-hard-deadline'; --no-pull never touches working-repo git state, --skip-failed self-heals SLUG_MISMATCH-style wedges in one tick) and gbrain-dream daily 05:00 ('gbrain dream' then graphify rebuild + cluster-only). Verified live via 'schtasks /Run': a plain sync BLOCKED on a new custom-slug docs file, the --skip-failed variant acknowledged and advanced the bookmark. PGLite quirks: scheduled runs exit nonzero harmlessly while a 'gbrain serve' MCP session holds the single-writer lock; embed backlogs queued as jobs never drain (no PGLite worker) - 'gbrain jobs cancel <id>' then 'gbrain embed --stale' drains inline, or the nightly dream absorbs them. Nightly probes enabled: autopilot.nightly_quality_probe + conversation_parser_probe. Steady state after full caching: ~1605 pages / ~29.3k chunks / ~97.9% embedded across shaft-engine + shaft-userguide; Links=0 remains a known limitation for federated code brains ('extract' finds no wikilinks; 'reconcile-links' is in the source CLI dispatch but missing from the installed binary's command registry).