When asked to fix "CI failures" spanning many unrelated jobs/platforms, group failures by exact exception signature first, then classify each group as code-defect / stale-already-fixed / external-provider before proposing any fix, per the ci-failure-investigator playbook. Cross-check a sub-agent's timing/log claims against raw `gh run view --log` output before committing to a workflow-level fix -- these are higher-blast-radius than app code and worth the extra verification pass.