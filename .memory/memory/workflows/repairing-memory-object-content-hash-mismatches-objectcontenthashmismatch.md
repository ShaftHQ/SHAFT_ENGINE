content_hash = 'sha256:' + sha256(canonicalJson(sidecar without content_hash) + '
' + normalizedMarkdown), where canonicalJson sorts object keys recursively with JSON.stringify escaping, and normalizedMarkdown strips a leading BOM and converts CRLF/CR to LF (from @aictx/memory dist/cli/main.js: computeObjectContentHash/normalizeMarkdownForHash). To repair: reimplement in Node, FIRST validate against all objects (2026-07-17: 136/138 reproduced exactly; only the 2 flagged mismatches differed), then rewrite only the stored content_hash string in the .json sidecar and rerun `memory check`. Do not hand-edit .md bodies without recomputing the hash.