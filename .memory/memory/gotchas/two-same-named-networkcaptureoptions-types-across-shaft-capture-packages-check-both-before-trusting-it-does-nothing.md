com.shaft.capture.runtime.NetworkCaptureOptions (mutable class, Spring-AI-bound MCP request DTO, all fields default meaningfully e.g. enabled=true, captureRequestBodies=false) and com.shaft.capture.network.NetworkCaptureOptions (immutable record consumed by CaptureNetworkRecorder) are hand-translated field-by-field in ManagedCaptureRecorder.translateNetworkCaptureOptions with NO compiler guard -- a field present on one side and absent on the other compiles cleanly and silently drops data. Before assuming a dropped field is 'probably redundant', trace: (1) is the field's gate already applied earlier in the call chain under a DIFFERENT name (e.g. apiCapture vs networkOptions.enabled), and (2) do any callers outside shaft-mcp (shaft-intellij plugin UI, e2e test fixtures, tool-index.json recorded examples) already send that field expecting it to do something -- that is strong evidence of a live contract, not a hypothetical.