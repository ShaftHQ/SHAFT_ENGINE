LookAndFeelIsolationExtension (shaft-intellij/src/test/java/com/shaft/intellij/ui/LookAndFeelIsolationExtension.java, issue #3782) snapshots/restores the exact pre-test LookAndFeel instance, the JBColor dark flag, and a declared set of UIManager keys on the EDT -- the same mechanism PR #3781's inline finally block proved sufficient for (5/5 consecutive green full-module gradlew test --offline runs). Live-reproduced (2026-07-19, filed as issue #3786): running the WHOLE module's test suite together with the gated ShaftPluginScreenshotRendererTest#rendersFeatureCatalogScreenshotsWhenOutputDirectoryIsProvided body actually enabled (gradlew --offline test -Dshaft.intellij.screenshotDir=<dir>) still fails 4 tests in ShaftTestsPanelTest with AssertionError at ShaftTestsPanelTest.java:327 (JTree.setSelectionPath inside DefaultTreeUI#repaintPath). Root cause: IntelliJ's DefaultTreeUI only becomes the active TreeUI delegate once a platform L&F has been installed AT LEAST ONCE in the JVM -- that is a JVM-wide sticky latch, not tied to which LookAndFeel object UIManager.getLookAndFeel() currently reports, so restoring the L&F instance afterward does not un-stick it. The documented, actually-used regen workflow (--tests ShaftPluginScreenshotRendererTest -Dshaft.intellij.screenshotDir=...) is unaffected (verified green) because it never shares a JVM with ShaftTestsPanelTest; only a bare full-suite gradlew test run combined with screenshotDir trips this. PR #3781's own '5 consecutive full-module green' claim never actually set -Dshaft.intellij.screenshotDir during those runs, so it never exercised this combination either -- treat that claim as verifying the ordinary (non-screenshot) path only.