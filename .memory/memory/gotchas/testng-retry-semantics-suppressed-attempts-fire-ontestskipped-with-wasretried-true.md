TestNG 7.12 TestInvoker.handleInvocationResult coerces every attempt that RetryAnalyzer retries into ITestResult.SKIP with wasRetried=true and fires onTestSkipped (carrying the real throwable); only the terminal attempt keeps FAILURE/SUCCESS and reaches onTestFailure/onTestSuccess. Any listener that counts or reports per-result must guard on !testResult.wasRetried() (precedents: IssueReporter.updateIssuesLog, TestNGListener.onTestSkipped since #3810). Dedup by ITestNGMethod is WRONG: it breaks retried-then-passed reporting and collapses data-provider rows.