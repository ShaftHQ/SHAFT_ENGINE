.claude/hooks/guard.py check_r7_orchestration_skill (around lines 695-715, self-tested at 1341) treats agent_type 'chaos-engine' — alongside '' and None for the main thread — as the orchestrator identity permitted to invoke the orchestration-only skills work-github and loop, while coder/reviewer/tester are structurally denied.
Practical consequence: .claude/agents/chaos-engine.md is a LIVE enforcement identity, not just documentation. Deleting it or renaming the agent removes the spawnable sub-orchestrator type and strands the R7 allowlist, and the breakage is silent — validators stay green. During #4260 the charter content was folded into the act-as-mohab skill but the file was deliberately kept as a thin pointer with the name unchanged, for exactly this reason.