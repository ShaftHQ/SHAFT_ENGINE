`validate_routing_bridges()` is defined at scripts/ci/validate_agent_guidance.py:383 and called at :509 (inside that same file's `validate_repository`). `scripts/ci/validate_agent_setup.py` only re-exports helpers from validate_agent_guidance (`expand_globs`, `load_budget`, `parse_frontmatter`, and `validate_repository` aliased as `validate_guidance`) -- it never calls `validate_routing_bridges` directly. A dispatch spec naming validate_agent_setup.py for routing-bridge work costs a blocked round-trip.
Also confirmed: a repo-wide search for `validate_routing_bridges` under `tests/` returns zero direct-test matches -- it is exercised only indirectly through `validate_repository`. Anyone changing routing-bridge parsing edits `validate_agent_guidance.py` and `tests/scripts/test_validate_agent_guidance.py`.