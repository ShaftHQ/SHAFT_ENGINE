ShaftTestIndex's old javadoc asserted that per-method SM parsing 'would pull in platform test-framework UI dependencies this lightweight plugin module intentionally avoids' -- this was not true for the currently pinned platformVersion (2024.3); see the companion memory on SMTRunnerEventsListener reachability. The javadoc was corrected in the #3688 fix (branch ChaosEngine/3688-live-sm-test-runner-status, merged via consolidated branch ChaosEngine/3678-3688-3690-intellij-hardening). Before citing an old rationale in this codebase for avoiding a platform API, re-verify it empirically rather than trusting the comment -- platform-version-pinned assumptions rot silently as platformVersion is bumped.