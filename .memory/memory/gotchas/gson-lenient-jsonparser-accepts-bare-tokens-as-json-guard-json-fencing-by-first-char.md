Gson JsonParser.parseString("Running") succeeds (lenient mode treats any whitespace-free token as a JSON string literal), so any 'is this JSON -> pretty-print and fence it' heuristic must first require the trimmed text to start with { or [. AssistantMarkdown.normalizeMarkdown fenced single-word milestone bubbles ("Running", "Denied") into broken ```json blocks until guarded (PR #3718).