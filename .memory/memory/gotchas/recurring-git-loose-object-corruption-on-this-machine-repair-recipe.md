This dev machine (BSOD history) has now corrupted git loose objects twice (a .corrupt.20260630 quarantine file existed; second incident 2026-07-19 hit blob 8fe14268 = current BrowserActions.java). Symptom: 'error: inflate: data stream error' / 'corrupt loose object' during git log -S or fsck. Repair: (1) git fsck --no-progress | grep -v dangling to find corrupt+missing objects; (2) git rev-list --objects --all | grep <sha> and git ls-files -s | grep <sha> to find what references it; (3) if the blob is the current version of a tracked file, verify git hash-object <file> equals the sha, then rm .git/objects/<xx>/<rest> and git hash-object -w <file> to rewrite it; (4) re-run fsck to confirm exit 0. If not recoverable from the working tree, use git fetch --refetch origin. Also delete stale *.corrupt.* quarantine files.