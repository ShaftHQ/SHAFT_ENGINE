Recorder payloads travel through up to three racing delivery channels (in-page queue drain, loopback sink POST, BiDi script channel). Byte-identical fingerprint dedup misses timestamp-drifted redeliveries and out-of-order step_delete revocations (double-click coalescing). CaptureEventPipeline keeps emittedClientActionIds for one-shot step kinds plus deletion tombstones so duplicates cannot append and deleted steps cannot resurrect; input keystroke merging deliberately reuses one clientActionId and stays exempt from the one-shot dedup.