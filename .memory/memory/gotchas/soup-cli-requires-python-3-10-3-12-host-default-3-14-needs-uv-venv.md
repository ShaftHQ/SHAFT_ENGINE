soup-cli 0.73.3 declares requires-python >=3.10,<3.13. This workstation default is Python 3.14.7. Install with uv CPython 3.12 under user-owned D:\AI\soup-poc\.venv, same pattern as Aider. Light install (no [train]) is enough for soup agent synth. Agent Forge works from cwd-contained OpenAPI; first attempt from the wrong cwd failed with Spec load failed. Live synth left tool arguments as empty {} — fill arguments in fixtures/harvest.