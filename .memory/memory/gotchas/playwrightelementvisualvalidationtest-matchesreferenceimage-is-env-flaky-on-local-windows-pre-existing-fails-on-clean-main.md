com.shaft.gui.playwright.validation.PlaywrightElementVisualValidationTest.matchesReferenceImageShouldAttachOnlyVisualComparisonWhenReferenceExists fails on this local Windows box regardless of branch — verified by stashing ALL working-tree changes and running against pristine origin/main, where it still fails at the assertTrue(attachmentsCaptor.getValue().isEmpty()) line. It is environment-sensitive (image/visual-comparison path) and green in CI. Do NOT chase it when it appears during unrelated validation/reporting work; verify baseline by stashing (git stash -u) and re-running before spending time. The sibling WebDriver visual test (visualValidationImageBytesShouldUseVisualComparisonAttachmentOnly in ValidationsHelperNewPatternCoverageUnitTest) DOES pass locally, so a single-test red here is expected, not a regression signal.