Grok projectTrusted / loaded-hooks probe cannot be healed by the installer (`/hooks-trust` is host-gated). Surfacing it as recovery-required made successful installs look broken and onboarding copy claimed doctor stays recovery-required. Keep grok_runtime_status / hostEnvironment as sync-advisory when install-owned hosts verify is healthy; never flip overall doctor to recovery-required for Grok trust alone. Onboarding gap text must match. See host-parity GAP-HOOK-TRUST and Grok onboarding card. Issue #5791.
