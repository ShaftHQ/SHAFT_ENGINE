Adding matrix, preflight, and operator checklist prose pushed the omniroute SKILL.md over the 20k byte Agent Guidance Gate until overlapping dispatch checklist text was compressed while preserving catalog-test contract phrases. When extending any ChaosEngine skill body, compress overlapping sections in the same PR and re-run skill phrase contract tests plus `python3 scripts/ci/validate_agent_setup.py --skip-external` before push. Issue #5767.
