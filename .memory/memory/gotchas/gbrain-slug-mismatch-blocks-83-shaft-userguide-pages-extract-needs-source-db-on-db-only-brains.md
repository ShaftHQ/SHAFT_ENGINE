gbrain's markdown lint requires frontmatter slug == path-derived slug; 83/187 shafthq.github.io files (all blog posts + custom-slug docs) declare Docusaurus slugs that ARE the public URLs, so they fail SLUG_MISMATCH and are skipped (synced with --skip-failed; failures in ~/.gbrain/sync-failures.jsonl; tracked as SHAFT_ENGINE issue #3618). Never run 'gbrain frontmatter validate --fix' on the docs repo. Related: 'gbrain extract links/timeline' defaults to source fs which walks local .md files and produced 0 links on this DB-only PGLite brain - pass '--source db' to extract from engine pages.