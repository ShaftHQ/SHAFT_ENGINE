PR #2895 ("optimize full page screenshot with cdp", commit e9c6f9d2e8) rewrote ScreenshotHelper.takeFullPageScreenshotUsingCDP and silently deleted the UnsupportedCommandException catch branch that #2841 (commit 3f5c397774) had deliberately added, shipping a months-long regression rediscovered as #3794 (fixed in PR #3797 by restoring the clause + a regression unit test). When reviewing/authoring refactors of exception-handling code, git log --follow the file and diff the catch clauses against the prior bugfix commits, and always leave a regression test behind so the branch can't be dropped silently again.