A managed compose can checksum/install as READY while `docker compose up` rejects it because a `depends_on: condition: service_healthy` target has no healthcheck. Independent review caught this on #5045: api waited on jobs healthy, jobs had no healthcheck. Bind with a test that every service_healthy target in the rendered YAML defines healthcheck:. Official ReportPortal jobs healthcheck is curl/wget on localhost:8686/health.