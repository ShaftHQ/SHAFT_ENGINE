A new @Service with @Tool methods in shaft-mcp is silently absent from the MCP API unless added to the shaftTools @Bean parameter list + ToolCallbacks.from + addAll chain in ShaftMcpApplication.java. Separately, every new tool needs an entry (name + mutation/sensitive/deprecated booleans) in shaft-mcp/src/test/resources/fixtures/mcp-tool-manifest.json or ShaftMcpApplicationTests.contextRegistersExpectedLeanMcpToolApi fails -- and that test is NOT in the PR CI path, so drift accumulates silently (10 tools drifted across PRs #3454/#3459/#3462; fixed in #3464). Related: the shaft-skills tool catalog regeneration gotcha.