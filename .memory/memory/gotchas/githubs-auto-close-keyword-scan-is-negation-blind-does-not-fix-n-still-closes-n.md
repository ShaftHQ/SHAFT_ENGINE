PR #4009's body explicitly said 'no closing keywords used, on purpose' and '**Does not fix** #3930's original macOS OOM -- that issue is being reopened separately.' Despite that, `gh pr view 4009 --json closingIssuesReferences` lists #3930: GitHub's keyword scanner matched the substring `fix #3930` inside `Does not fix #3930` with no awareness of the preceding negation. Verified via `gh api repos/ShaftHQ/SHAFT_ENGINE/issues/3930/timeline`: a human manually reopened #3930 at 2026-07-25T20:47:17Z with an explicit comment ending 'Leaving open.', then GitHub auto-closed it again at 2026-07-25T20:54:36Z -- 2 seconds after PR #4009 merged (`mergedAt: 2026-07-25T20:54:34Z`) and about 7 minutes after the manual reopen -- because that PR's body still contained the 'fix #3930' substring. The defect #3930 tracked (macOS heap OOM in MacOSX_Safari_Local) sat wrongly closed while nightly runs kept failing on it. Fix tracked as #4142. The only safe way to mention an issue in a PR body/commit that you are NOT closing is a bare `#N` with no keyword (fix/fixes/close/closes/resolve/resolves) anywhere before it on the same line/sentence -- not even inside a negated phrase like 'does not fix'.