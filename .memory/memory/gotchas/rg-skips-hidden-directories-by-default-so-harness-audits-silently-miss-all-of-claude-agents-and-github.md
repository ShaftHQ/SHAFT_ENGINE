Auditing the agent-guidance surface with a plain `rg -n "act-as-fable" .` returned 4 hits (AGENTS.md, CLAUDE.md, scripts/ci/agent_guidance_budget.json). The same search with `--hidden` returned 30, because every harness directory is a dot-directory: .claude/skills, .claude/agents, .claude/hooks, .claude/user-harness, .agents/skills, .github/skills. The 26 missed hits included .claude/hooks/guard.py:1003 (a live hardcoded skill path), all four agent charters, the work-github playbook, the caveman skill, and .claude/user-harness/CLAUDE.md.
Practical consequence: any 'nothing references this, safe to delete' conclusion drawn from an rg sweep of the harness WITHOUT --hidden is a false clean and can orphan live references. Always pass --hidden (and exclude .git/.memory explicitly) when auditing agent guidance. Verified 2026-07-27 during #4260.