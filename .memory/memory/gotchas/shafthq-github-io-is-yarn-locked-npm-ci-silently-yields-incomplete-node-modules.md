The docs repo commits both yarn.lock (used by CI: yarn install + yarn build) and a stale package-lock.json. npm ci exits 0 but node_modules lacks typescript (tsc not found) and @google/genai (test:docs fails). Use yarn install --frozen-lockfile for local verification. Also: combined typecheck+build locally exceeds a 10-minute tool window -- run typecheck and build as separate steps. Filed as shafthq.github.io#825.