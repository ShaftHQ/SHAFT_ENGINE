scripts/agents/sync_user_harness.py MANIFEST = ('CLAUDE.md', 'settings.json', 'statusline-command.sh') plus every .claude/agents/*.md, deploying from .claude/user-harness/ to ~/.claude/ (agents land in ~/.claude/agents/). So the user-global shim, including its absolute pointer at .claude/skills/<skill>/SKILL.md, is edited IN-REPO and then deployed with --apply; editing ~/.claude/CLAUDE.md directly creates drift the script reports as DRIFTED.
Note the script does NOT deploy .claude/skills/ — that is why the shim points at the absolute SHAFT_ENGINE path for the skill rather than a ~/.claude copy, and why renaming a skill directory requires updating that absolute path. Verified 2026-07-27 during #4260.