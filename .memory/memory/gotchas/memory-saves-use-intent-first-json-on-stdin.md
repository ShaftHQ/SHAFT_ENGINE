This repo's installed Memory CLI supports routine writes with `memory remember --stdin` and optional `--dry-run`; structured patches use `memory save --stdin` or `memory save --file`. Do not rely on stale template examples such as `memory save --dry-run`, `memory save --id`, or free-form `memory save` text arguments.