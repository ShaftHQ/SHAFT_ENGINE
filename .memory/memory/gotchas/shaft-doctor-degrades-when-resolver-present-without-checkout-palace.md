If `tools/repository-map/resolve_mempalace.py` is a file and checkout `.chaos-engine-state/mempalace` is missing, `mempalace_runtime_status` is `degraded` (not `initialization-required`) and `initialize_mempalace_runtime` is a no-op. Detail must name `scripts/agents/knowledge_stores.py status`. Do not hard-wire `hosts.py` or `tool.py --palace` to `<git-common-dir>/chaos-engine/mempalace`. Do not add a stub `degraded` row to the existing `test_chaos_engine_dependencies` tool-guard: parent already refuses any non-healthy status, so `validate_red_before_green.py` would reject that new method as already green.