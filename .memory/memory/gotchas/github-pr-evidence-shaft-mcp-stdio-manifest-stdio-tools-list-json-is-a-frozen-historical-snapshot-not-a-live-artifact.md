This file is written only by ShaftMcpStdioManifestIT (an @Tag("external-e2e") integration test) to target/shaft-mcp-evidence/stdio-tools-list.json, then manually copied into .github/pr-evidence/ as PR evidence for issue #3338 (2026-07-06, per its sibling evidence-manifest.json). Nothing in production or test code reads it back at runtime -- it is not consulted by any live-serving code path. It goes stale immediately after the PR that captured it: confirmed it predates the 2026-07-17 fix that made capture_start's targetUrl/browser/outputPath/sessionGoal optional (it shows all of them, plus headless, as required, and is even missing the sessionGoal property entirely). Do not treat files under .github/pr-evidence/ as current schema/behavior truth; they are point-in-time archives. The live source of truth for a tool's served JSON schema is the Spring-managed shaftTools/toolSpecs beans (see ShaftMcpApplicationTests), not any checked-in snapshot.