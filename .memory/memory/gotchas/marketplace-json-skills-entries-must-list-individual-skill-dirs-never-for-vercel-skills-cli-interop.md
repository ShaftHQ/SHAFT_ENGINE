Counts refreshed 2026-08-09 against the live tree for issue #4649. The canonical repo-local maintainer skill set is one: `.agents/skills/act-as-mohab/SKILL.md`; consult-first and retrieve-first are internal references, not discoverable skills. `.claude/skills/act-as-mohab/SKILL.md` is a thin host redirect to that same logical skill. Generic skills CLI convention scanning may therefore show both host paths, but they resolve to one repo-local skill. The 11 SKILL.md under `.github/skills/` remain outside the conventional `.agents/skills/` and `.claude/skills/` scan roots. Marketplace manifests must still list each intended packaged skill directory explicitly and never use `./`.