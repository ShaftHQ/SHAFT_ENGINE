The reported web-recorder bug (extra 'Navigate to' rows after an ENTER-triggered DuckDuckGo search) does NOT reproduce via the documented hypothesis (a genuine second top-level navigation via location.replace after a 302). A new E2E fixture (search-home-replace: form GET -> 302 -> results page -> location.replace canonicalization) records only the initial open: both consequence commits arrive with interactionConsequence=true (58-107ms after ENTER) and are suppressed by BOTH the server BiDi 'default' branch (CaptureEventPipeline.emitNavigation) AND the client URL-poll gate (shaft-capture-recorder.js:2590, sinceInteraction>10s else noteIgnored). Those suppressions landed in #3495 (server) and #3508/#3496 P0 (client) on 2026-07-13; the bug report post-dates them. The one UNCONDITIONAL append path is popstate -> reportNavigation('user_traversal') (recorder.js:2605) which the server user_traversal branch appends without the interactionConsequence check -- this is DELIBERATE so genuine back/forward is recorded, and fixing it blindly risks the back/forward-recording tests. Residual DDG-specific trigger is unreproduced synthetically (SPA pushState is caught by the URL-poll gate; location.replace is a full nav, not popstate). To pin it down, capture the REAL DuckDuckGo flow live rather than guess. A regression test (searchWithSecondReplaceNavigationRecordsOnlyUserPerformedSteps) now guards the location.replace case.