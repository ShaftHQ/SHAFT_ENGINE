RESOLVED 2026-07-17 (issue #3625, commit 3c96c3fb3c on codex/phase2-trust-status-truth): recommendation is no longer final. The two combo addActionListener callbacks (family, runtime) now recompute it via recommendation = recommendFamily(currentSelectionSnapshot()) before calling assistantSelectionChanged(); currentSelectionSnapshot() builds a fresh ShaftSettingsState.Settings from the live combo values. The constructor's original recommendation = recommendFamily(settings) (computed once) is unchanged, so the probe-runs-once cost on plain construction is preserved -- only genuine combo interaction re-runs recommendFamily. recommendedAgentText() itself is unchanged; it just now reads a value that tracks live selection. The two-instance test workaround from the original gotcha is obsolete: ShaftPanelSetupTest.recommendedAgentAccessibleDescriptionTracksLiveRecommendationAcrossConstructionAndSummaryUpdate now asserts the description changes (assertNotEquals) after a combo interaction, on a single panel instance, instead of asserting it stays frozen.