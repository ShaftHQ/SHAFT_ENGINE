AssistantCommandTest.localAgentPromptsIncludeGenericShaftMcpUsageHint() asserts the FULL local-agent prompt text with assertEquals("""...""", ...) in two places (the 'duckDuckGo' and 'plain' cases), each embedding the entire SHAFT_MCP_USAGE_HINT constant verbatim. A third case ('alreadyExplicit', where the user's own prompt text already contains 'shaft-mcp') bypasses the hint entirely and is unaffected. Editing SHAFT_MCP_USAGE_HINT's text (as issue #3674/PR #3689 did, appending a shaft-options clarifying-question instruction) breaks both exact-match blocks unless updated in lockstep. Grep the whole test tree for the constant's leading sentence ('If this request requires interacting with a browser') before editing it, since other files could theoretically pin it too (none currently do outside this one test method).

STALE (confirmed 2026-07-27, during #4258): the exact-match assertEquals("""...""") pattern this describes no longer exists in AssistantCommandTest.java. Between #4239 P2.2 (PR #4257) and #4256/#4258, SHAFT_MCP_USAGE_HINT was split into _LOCAL_AGENT/_CLOUD variants and the test assertions were rewritten to assertTrue/assertFalse(...).contains(...) checks. The lockstep-editing risk this gotcha warned about no longer applies to the current test file.