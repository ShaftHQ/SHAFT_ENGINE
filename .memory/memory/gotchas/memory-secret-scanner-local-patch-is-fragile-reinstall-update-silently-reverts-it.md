The openai-key false-positive fix (issues #4005/#3993: the openai_api_key detection pattern matched mid-word inside ordinary hyphenated words like "task-", tripping MemorySecretDetected on unrelated body_path values) is applied ONLY as a direct edit to the globally-installed npm package, not a source-controlled fix: C:\Users\Mohab\AppData\Roaming\npm\node_modules\@aictx\memory\dist\cli\main.js, anchoring the pattern with a leading word-boundary so it can no longer match mid-word. A timestamped backup of the original file was left alongside it before editing (exact backup filename is recorded in the #4005 comment thread). Any `npm i -g @aictx/memory` or version update overwrites dist/cli/main.js and silently reverts the patch -- there is no warning, just a return of the bug. Symptom on regression: `MemorySecretDetected` on a body_path field (not content_hash), hard-blocking `memory check` and `memory load` again. Upstream fix branch, TDD'd and pushed but not mergeable under this identity (aictx/memory restricts PR creation to collaborators): https://github.com/MohabMohie/memory/tree/fix/openai-key-pattern-word-boundary. If memory check starts hard-failing with MemorySecretDetected again, re-check whether this local patch survived the last npm update before re-diagnosing from scratch.