modular-era-feature-catalog.md references screenshots two ways: as real Markdown/HTML images, and as bare backticked filenames in a 'Proof' table column. A link checker that only parses image and link syntax reports 0 missing while a referenced PNG is genuinely absent -- a Haiku delegate reported 'all 55 image references exist' and was wrong. Verified by diffing every PNG-filename match in the doc against a directory listing of shaft-engine/src/main/resources/modular-era-feature-catalog/: intellij-plugin-assistant-approval-prompt.png was referenced but missing, and intellij-plugin-tools.png / intellij-plugin-tools-dark.png are on disk but unreferenced. Always diff BOTH directions (referenced-but-missing AND on-disk-but-orphaned) by filename, not just by parsed link syntax.