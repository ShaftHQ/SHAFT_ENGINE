Graphify refresh is one fail-closed critical section. The repository-owned tools/repository-map/graphify_maintenance.py controller must acquire a cross-process, crash-released OS advisory lock before invalidating the freshness marker and hold it through build -> coverage audit -> cluster -> record. Contention must fail before any cache mutation; build, audit, cluster, or record failure must leave the prior marker absent, and process exit must release the kernel lock so a killed nightly task cannot block refresh permanently. The Windows tools/agent-infra/graphify-refresh.cmd adapter remains thin and must reject both positive and negative controller exits (`if errorlevel 1` plus `if not errorlevel 0`). Keep concurrency, linked-worktree, partial-stage, marker, and Windows wrapper regressions in tests/scripts/test_graphify_maintenance.py and tests/scripts/test_resolve_graph_out.py, reached by the Agent Guidance PR-gate filter. Established by #4639 and strengthened by #4687 / PR #4748.