The daily tools/agent-infra/graphify-refresh.cmd must record the Graphify marker only after both the full build and cluster-only commands succeed. In cmd.exe, `if errorlevel 1` catches positive failures but misses negative native crash statuses, so each stage needs both `if errorlevel 1` and `if not errorlevel 0` gates. Keep the real Windows regression in tests/scripts/test_resolve_graph_out.py and the Agent Guidance path trigger in .github/workflows/pr-gate.yml. Established by SHAFT_ENGINE issue #4639 and commit b5f65b7c29.