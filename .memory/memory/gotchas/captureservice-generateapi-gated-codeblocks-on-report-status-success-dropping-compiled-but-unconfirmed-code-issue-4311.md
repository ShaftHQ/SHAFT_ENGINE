shaft-mcp/src/main/java/com/shaft/mcp/CaptureService.java:718-721 (generateApi, the capture_api_generate tool) computed `successful = report.status() == SUCCESS` and gated the returned codeBlocks on that alone. Since capture_api_generate defaults replay=false (issue #4220), a genuinely working generation reports status=UNCONFIRMED (not SUCCESS) even when compilation passed -- so codeBlocks always came back empty for the default (no-replay) call. This is the exact same bug class already fixed for the WebDriver/Playwright codegen path via CaptureService#generatedCodeUsable (issue #3409: gate on report.status()==SUCCESS OR compilation.status()==PASSED, not SUCCESS alone), but that fix was never applied to the newer API codegen path added for #4220. Caught by the 'Guided Workflows Live E2E' nightly (run 30331796055): GuidedWorkflowLiveE2ETest#apiRecorderCapturesNetworkTrafficAndGeneratesShaftApiCode failed at GuidedWorkflowLiveE2ETest.java:209 asserting the generated code contains 'new SHAFT.API(' -- codeBlocks was []. Fixed by extracting a shared `sourceUsable(Path sourcePath, CaptureGenerationReport report)` helper used by both generateApi and generatedCodeUsable. Regression test: CaptureServiceApiToolsTest#generateApiReturnsCodeBlocksWhenCompilationPassedButReplayWasSkipped (shaft-mcp/src/test/java/com/shaft/mcp/CaptureServiceApiToolsTest.java) reproduces this with a real recorded session and real ApiCaptureGenerator, no mocks. Lesson: whenever a new codegen/tool path introduces its own successful/codeBlocks gate, check it against the UNCONFIRMED-status precedent (#3409/#4220) instead of re-deriving status==SUCCESS from scratch.