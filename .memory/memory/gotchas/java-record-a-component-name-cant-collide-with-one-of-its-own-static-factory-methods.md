record Result(boolean ok, String reason) with a static factory also named Result.ok() fails to compile: the record auto-generates a public instance accessor ok() that collides with the static method of the same name (javac: "invalid accessor method in record Result"). Name the boolean component something else (e.g. success) and keep the static factory name (ok()/fail(reason)) if that reads better at call sites.