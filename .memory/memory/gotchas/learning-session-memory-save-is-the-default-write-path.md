After Memory migrate (#5845 / #5846 merge SHA 1ac90e1), Learning Sessions must use `memory save --stdin` as the default write path for durable gotchas, decisions, and facts. Do not hand-edit `.memory/**` JSON or markdown sidecars (the temporary runtime-shaped manual writes used for #5848–#5851 while the legacy corpus blocked save). If save rejects, report the reason and fix the intent JSON; never bypass by authoring sidecars. Intent shape: `{task, nodes, stale, supersede, delete}`. Issue #5852.