Ranking CLI models JSON then launching omniroute run still failed with catalog-id misses, rate limits, and claude plus Kimi unrecognized_model hangs because catalog display ids are not live completion ids and claude only accepts Claude-family ids. Keep productive dispatch in runner.py: per-provider live models list, CLI target matrix (claude for Claude-family only; otherwise opencode or codex), prefer auto/coding with smoke preflight, and zero same-identity retries on rate-limit, catalog-miss, or unrecognized_model. Issue #5769; program #5764.
