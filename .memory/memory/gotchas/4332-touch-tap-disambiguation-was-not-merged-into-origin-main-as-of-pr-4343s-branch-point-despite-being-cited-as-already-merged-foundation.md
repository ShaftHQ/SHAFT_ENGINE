The #4327 dispatch prompt stated origin/main included '#4332 (touch/tap disambiguation, merged)' among the foundation this task should build on. Verified via `git merge-base --is-ancestor 21f0c313e7 origin/main` (the tip of the ChaosEngine/touch-tap-disambiguation-4332 worktree/branch) immediately after `git fetch origin`: NOT an ancestor. `git log origin/main --oneline` at the time (tip 8a95cf11f8) confirmed #4321/#4326, #4328/#4331, and #4334/#4338 were all present, but no #4332 merge commit existed anywhere in origin/main's history. TouchMultipleElementsFailureTest (the regression suite #4327 was asked to also verify) does not exist on origin/main for the same reason. Before assuming a dispatch prompt's claimed merged-foundation list is accurate, verify each cited PR/issue independently against a fresh `git fetch origin` + `git log origin/main` -- a stale or in-flight PR can be described as merged when it is still open.