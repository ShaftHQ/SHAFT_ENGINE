AutobotService and McpDoctorRemediationService SECRET_LIKE must match the value after authorization or api-key labels, including Basic or Bearer scheme and quoted or unquoted tokens. A label-only group leaves the credential in the captured AiRequest. The matched token tail must allow hyphen (`[a-z0-9._-]{8,}`), not letters and digits only, or hyphenated vendor keys remain after Bearer redaction. FAILED_LOCATOR must allow quotes around selector so JSON Allure locators are stripped. Evidence: AutobotServiceTest and McpDoctorRemediationServiceTest failed on 2026-08-18 until both patterns consumed values. PR https://github.com/ShaftHQ/SHAFT_ENGINE/pull/5122