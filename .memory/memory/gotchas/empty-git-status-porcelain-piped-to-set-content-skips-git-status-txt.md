In run_agent.ps1, `git status --porcelain | Set-Content git-status.txt` writes no file when Aider auto-commits and porcelain is empty. agent.py changed then FileNotFoundErrors and no report.json is written. Capture status text first and Set-Content -Value, treating a missing sidecar as empty. Observed on #5144 reports 20260818T160922Z and 20260818T165603Z. Fixed in 3ffaa0757e / PR #5168.