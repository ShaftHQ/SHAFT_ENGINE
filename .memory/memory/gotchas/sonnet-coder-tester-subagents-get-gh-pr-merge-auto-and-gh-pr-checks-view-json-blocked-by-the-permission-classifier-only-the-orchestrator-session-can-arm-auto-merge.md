Across 4 separate delegated coder/tester subagents in one session (fixing #4308, #4310, #4311, #4316), every single one reported `gh pr merge <n> --auto --squash` and often `gh pr view --json`/`gh pr checks` denied by the Claude Code auto-mode permission classifier, even though the exact same commands succeed instantly when run from the orchestrator's own main-thread session. Each delegate correctly stood down rather than working around the denial (per the covenant), reported it plainly, and left the PR open/unmerged for the orchestrator to arm.

Why: this is not a repo/gh-auth issue -- it's session-tier: subagent sessions apparently sit under stricter auto-mode classification than the main orchestrator thread for this specific action shape (merge-affecting gh commands). It reproduced 100% of the time (4/4), so treat it as a standing constraint, not a fluke to keep re-discovering.

How to apply: don't brief delegated subagents to arm auto-merge or expect them to self-verify CI green via gh -- brief them to open the PR referencing "Fixes #NNNN" and stop there (as already instructed in this session's dispatch prompts, e.g. "do not merge yourself"). The orchestrator (main thread) must always do the `gh pr checks`/`gh pr view --json` verification and `gh pr merge --auto --squash` arming itself as a matter of course after every subagent reports a PR URL, not just when a delegate happens to ask for help.