SHAFT stores Flutter server and element wait timeouts in seconds while Appium Flutter options expect milliseconds. Blindly copying the property bag onto Appium capabilities overwrites a correct millis value with the raw seconds integer, so cold starts die early with `Flutter server not started`. Convert seconds to milliseconds when copying into Appium capabilities. Issues #5875 / #5876; PR #5886.
