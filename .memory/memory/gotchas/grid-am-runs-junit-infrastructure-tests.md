Native Selenium Grid jobs in e2eTests.yml must not use mvn -pl shaft-engine -am test. shaft-infrastructure is JUnit with failIfNoSpecifiedTests=false, so TestNG GLOBAL_TESTING_SCOPE !%regex exclusions do not filter it. SeleniumGridLifecycleServiceTest then runs against the job's already-started selenium4.yml Grid, fails, and stops the reactor before shaft-engine E2E (no JaCoCo/Allure). Those tests belong in pr-gate unit-tests for shaft-infrastructure. Fix: drop -am on Grid Run tests; visual-runtime already did mvn -pl shaft-visual -am install -DskipTests. Adding another !%regex cannot work.