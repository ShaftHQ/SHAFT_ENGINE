Surefire in SHAFT_ENGINE does not fail the build on test failures: a run with failures=1 still prints BUILD SUCCESS (observed 2026-07-12 while fixing #3466). After any scoped `mvn test`, verify the verdict with grep 'tests=' shaft-engine/target/surefire-reports/TEST-*.xml (or the .txt summaries) and confirm errors=0 failures=0 and that the counts match the intended scope. Never report a green verdict from BUILD SUCCESS alone.