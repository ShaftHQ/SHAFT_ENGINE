AbstractHttpAiProvider#outputTokenLimit and AiExecutionService#inheritedLimit both compute min(requested, PilotConfiguration.current().maxOutputTokens()). AutobotService.java:167 raised its per-request AiBudget to 8,000 in PR #3371 to stop Gemini reasoning-model truncation, but Pilot.java's global pilot.ai.maxOutputTokens default stayed at 2,000 (never touched by #3371/#4072/#4107). Every call was silently clamped back to 2,000 the whole time -- the '8k fix' never took effect. Verified via GeminiProviderTest#buildPayloadHonorsTheRequestedOutputBudgetInsteadOfTheGlobalDefaultCeiling (red at 2000, green at 8000 after raising Pilot.java's @DefaultValue). Lesson: raising a per-request AiBudget field means nothing unless the global PilotConfiguration ceiling is checked/raised too -- grep both sides of any min(requested, configured) clamp before trusting a 'raised the budget' fix.