1) location.hash changes are same-document navigations that fire popstate, so the overlay's traversal handler (reportNavigation user_traversal) consumes them and updates uiState.lastUrl before the 500ms URL poll ticks — the poll's consequence-suppression branch (and anything keyed to it, like the 'Ignored: navigation caused by your last step' note from #3496 P0) never runs for hash-only changes. Demo/exercise consequence suppression with a real document navigation ~400ms after a recorded interaction. 2) In-page loopback fetches to BrowserEventSink (/steps, /session) are best-effort by architecture (see ManagedCaptureRecorderBrowserTest's waitForBestEffort note): overlay features fed by them need fetch-less fallback copy, and e2e assertions on fetch-delivered content must be best-effort; server-side store assertions are the authoritative proof.