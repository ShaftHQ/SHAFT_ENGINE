When multiple active Maven profiles configure the same maven-surefire-plugin list, do not assume Maven appends their entries. In the visual/OCR acceptance profiles, separate additionalClasspathDependencies configurations caused one runtime module to replace the other. Restate all required entries explicitly in the later active profile and verify the resolved list from help:effective-pom; a source-POM inspection alone is insufficient.