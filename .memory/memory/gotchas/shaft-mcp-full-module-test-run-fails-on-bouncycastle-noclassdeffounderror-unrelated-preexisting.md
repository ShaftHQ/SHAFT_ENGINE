2026-07-17: running the full shaft-mcp module test suite (mvn -pl shaft-mcp -o test, no -Dtest filter) fails with java.lang.NoClassDefFoundError: org/bouncycastle/jce/provider/BouncyCastleProvider inside MobileApiCaptureController's constructor (MobileService/MobileServiceConfigurationTest/MobileServiceDriverBackedTest/MobileServiceReplayTest, plus cascading ShaftMcpApplicationTests ApplicationContext load failures). Confirmed via git stash that this reproduces identically on completely unmodified shaft-mcp/shaft-capture source -- it is a pre-existing local .m2/classpath-resolution gap in this dev environment (bouncycastle jars ARE present under ~/.m2/repository/org/bouncycastle/, but shaft-mcp/pom.xml has no direct bouncycastle dependency, so it must come transitively and isn't resolving onto the effective test classpath in offline mode), not a code regression. Individual/scoped test classes not touching MobileService are unaffected and pass normally.