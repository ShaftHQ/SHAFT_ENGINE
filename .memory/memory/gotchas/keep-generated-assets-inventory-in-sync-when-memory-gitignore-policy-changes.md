Changing durable Memory from allowlisted to tracked drifted the README generated-assets inventory in scripts/ci/validate_chaos_engine_readme.py and failed Agent Guidance until that inventory row was updated in the same delivery. When changing .gitignore or hosts Memory tracking policy, update the generated-assets inventory (and any README inventory tables it renders) in the same PR, then run the README inventory unit checks before push. Issue #5776.
