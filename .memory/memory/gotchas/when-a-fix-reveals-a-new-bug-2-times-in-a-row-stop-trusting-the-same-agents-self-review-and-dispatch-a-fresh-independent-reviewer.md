On SeleniumHQ/selenium#17832 (2026-07-29), three consecutive rounds each had the SAME coder agent TDD-verify a fix (red then green, real tests, real evidence) that turned out to hide a new bug the next qodo-bot pass caught: Bug A's handler-stripping fix introduced duplicate log output; the duplicate-output fix introduced silent data loss from a configured log-file; that fix's own live-property-check introduced a race condition. Each individual fix was honestly verified and genuinely fixed what it targeted -- the failure mode was scope-blindness, not dishonesty: the same agent, deep in the same 2-3 files across many turns, kept missing the NEXT-order interaction its own fix created. Dispatching a fully independent reviewer agent (fresh context, no attachment to the code it's reviewing) for one full adversarial pass over the entire diff (not just the latest patch) found exactly one more real, narrow issue and confirmed everything else clean -- confidence a same-agent self-review round could not have provided.