Issue #3409: the capture overlay STOP control arrived on the polling collector own executor thread; ManagedCaptureRecorder.stop() closed that collector, whose shutdownNow() interrupted the executing thread, so later NIO session-store writes threw ClosedByInterruptException and WebDriver.quit() aborted - session stuck STOPPING (later INCOMPLETE), browser+chromedriver orphaned. Repro trick: a CSP page with connect-src self silently blocks the recorder loopback sink fetch, forcing control delivery through the polling channel. Fix: dedicated shaft-capture-ui-stop thread plus clear/restore Thread.interrupted() around teardown in stop()/interrupt().