Issue #4030 and its dispatch cited `.claude/skills/choosing-shaft-locators/SKILL.md:16` for the Smart-Locator-ranked-first ladder. That path does not exist. The real (and only) location is `shaft-skills/choosing-shaft-locators/SKILL.md`, alongside its siblings writing-shaft-tests, recording-shaft-tests-with-mcp, planning-shaft-tests, analyzing-shaft-failures, act-as-shaft-dev -- the whole SHAFT-project-facing test-authoring skill family lives in `shaft-skills/`, separate from `.claude/skills/` (Chaos Engine's own harness skills, e.g. shaft-mastery) and from `.agents/skills/` / `.github/skills/` (Codex-bridge skills, no locator content). Verify the actual path with `ls`/`rg` before trusting a cited line number in a ticket.