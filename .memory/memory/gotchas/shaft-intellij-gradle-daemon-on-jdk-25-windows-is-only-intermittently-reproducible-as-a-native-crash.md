Reproducing issue #3784 locally (Windows 11, OpenJDK 25.0.1 as JAVA_HOME, gradlew --offline test --tests ShaftPluginScreenshotRendererTest against commit cb5960e1fd): the daemon started and the full test task completed successfully, no EXCEPTION_ACCESS_VIOLATION observed on this run. The originally-reported crash (recorded when filing #3784) is real but not deterministically reproducible on demand; treat it as a G1 GC timing/memory-pressure-dependent native crash, not a guaranteed-every-run failure. This is why a fail-fast guard (reject the JVM version before the crash-prone IntelliJ Platform test execution phase runs) is a sound mitigation even without being able to force the crash on demand: the guard only needs to run before the risky code path, which it reliably does since settings.gradle.kts evaluates on the real Daemon JVM at the very start of the build, long before instrumentCode/test tasks execute.