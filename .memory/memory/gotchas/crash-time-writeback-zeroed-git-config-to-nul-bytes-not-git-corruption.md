On session resume every git command failed with `fatal: bad config line 1 in file .git/config`. Root cause was NOT git: `.git/config` was 3526 bytes, every byte 0x00 -- byte length preserved exactly, content zeroed (classic crash-time writeback zeroing; this machine has BSOD history, see [[recurring-git-loose-object-corruption-on-this-machine-repair-recipe]]). One-step diagnostic: `[System.IO.File]::ReadAllBytes(path) | Sort-Object -Unique` returned the single value `0`. Gotcha: a PowerShell `-replace '\s',''` length check does NOT reveal this -- `\s` does not match NUL, so the file looks whitespace-free and the byte count looks normal.
Recovery, in order: reconstruct from surviving evidence -- remote URL from `.git/FETCH_HEAD`, remote name from `.git/refs/remotes/`, per-worktree settings from `.git/config.worktree`, branch from `.git/HEAD`. `extensions.worktreeConfig = true` MUST be restored -- `config.worktree` is silently ignored without it, so per-worktree `hooksPath` stops applying with no error. Accepted at `repositoryformatversion = 0` on git 2.55.0.windows.3 (verified). Re-run `git lfs install --local` rather than hand-writing `[filter "lfs"]`. Verify with `git fsck`, `git worktree list`, `git config --worktree --list`, `git fetch --dry-run`.
Reassuring part: `git fsck` came back clean and a full scan found exactly one zeroed file -- the object database survived, only the config text was lost.