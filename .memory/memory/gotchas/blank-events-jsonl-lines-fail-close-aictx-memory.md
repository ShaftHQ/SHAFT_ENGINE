A single blank line in `.memory/events.jsonl` (observed at line 1176 between otherwise valid `memory.updated` / `memory.created` events) makes `@aictx/memory` fail closed with `MemoryInvalidJsonl: Blank JSONL lines are not allowed.` Search, inspect, and remember stay unavailable until the index can load. `scripts/ci/validate_agent_setup.py` `memory_update_timestamps` already skips blank and malformed lines because `merge=union` can insert them. The live blank was removed in commit 049373a690 (#5214). Recurrence remains possible from `merge=union`; do not rewrite history. Treat the store as degraded until a checkout has no blank JSONL lines.