When creating a linked Git worktree from the agent shell on Windows, pass the destination with forward slashes and invoke `git -c core.longpaths=true worktree add`. Backslashes are consumed by the Bash/MSYS boundary and the repository contains paths long enough for a plain checkout to fail. The R9 lifecycle refusal is correct; follow its corrected command rather than bypassing it.