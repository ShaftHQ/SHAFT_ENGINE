On origin/repository checkouts, curl install materializes `.chaos-engine/` from the remote payload while doctor `coreMatchesSource` compares owned overlay bytes to local `chaos-engine/` SOURCE. Even on a clean same-commit tree, owned paths (memory-v5 schemas, shaft playbooks) can diverge via materialize/EOL, leaving permanent `overlay-source-mismatch` until a reinstall dance that still may not sync from local SOURCE. After core install/rematerialize and once during doctor before recovery-required, deterministically copy `owned_source_files()` from `chaos-engine/` → `.chaos-engine/` (byte-identical) and rewrite overlay `manifest.json` files digests; only if heal is still impossible write `.chaos-engine-state/overlay-handoff.md` and one backtick-wrapped agentic prompt (no bare "Reinstall…" fixNext). Adopter projects (no SOURCE) unchanged. See chaos-engine/references/installer-program.md repository overlay sync. Issue #5795 (product #5794).
