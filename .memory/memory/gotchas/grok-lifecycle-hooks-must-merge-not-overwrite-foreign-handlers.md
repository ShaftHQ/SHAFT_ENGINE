Assigning the owned ChaosEngine hook document onto `.grok/hooks/lifecycle.json` wipes any pre-existing user handlers. Codex and Claude already go through `without_chaos_hooks` then `hook_content`. Grok must use that same merge. Evidence: independent review of #5091; RED was `test_grok_hooks_preserve_unrelated_events` seeing `user-hook` replaced by the locator command; fix `f8796432d2`.
