GeneratedTestValidator.replay passed ~385 Maven-repo jar paths inline in -cp, exceeding the ~32K Windows CreateProcess command-line limit, so every /codegen replay died at launch as a bare IOException (Generated test replay could not be launched). Fixed with a Java @argfile (same technique as the shaft-mcp launcher). Related: a replay failure must not swallow generated+compiled code blocks - CaptureService.generatedCodeUsable returns them whenever compilation passed, while generation-level (privacy gate) failures keep suppressing them.