Codacy flagged GeneratedCodeGuardrails.isSuspiciousSecretLiteral (shaft-capture) at NPath complexity 600 vs threshold 200 as a check-run annotation on PR #4249. Searched both the root pom.xml and shaft-capture/pom.xml for any pmd plugin config -- none exists anywhere in the repo. Codacy runs its static analysis entirely on Codacy's own infra (cloud check-run), not via a Maven-invoked PMD goal in this repo. To verify an NPath-reduction refactor here, you cannot rerun the exact rule locally: verify instead via (1) full existing-test-suite green before/after, (2) matching the file's own established low-complexity extraction pattern (e.g. GeneratedCodeGuardrails already used many single-purpose addXxxViolations helpers before this fix), and (3) manual reasoning that each extracted helper's own NPath is now trivially small since it isolates one logical group of conditions.