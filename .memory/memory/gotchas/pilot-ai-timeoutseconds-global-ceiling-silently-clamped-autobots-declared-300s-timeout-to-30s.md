Same defect class as gotcha.aibudget-per-request-output-tokens-are-dead-unless-global-default-keeps-up, now for the timeout knob: AutobotService declares a 300s per-request timeout but AiExecutionService#effectiveTimeout takes min(requested, PilotConfiguration.timeout()) and the Pilot.java pilot.ai.timeoutSeconds default stayed 30 -- every provider call was HTTP-capped at 30s. Latent until #4113 let the full 8k output budget reach the wire; reasoning-model codegen then ran past 30s -> status=TIMEOUT Provider request timed out (live Gemini E2E nightly). Fixed #4361 by raising the default to 300. Rule: when a per-request AI value (tokens, timeout, anything min-clamped by PilotConfiguration) is raised or declared, verify the matching pilot.ai.* global default keeps up, and add a default-value assertion in PropertiesUnitTest.