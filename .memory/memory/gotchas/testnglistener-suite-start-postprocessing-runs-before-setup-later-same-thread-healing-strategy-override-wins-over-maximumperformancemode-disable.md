Proven empirically in PropertiesHelperMaximumPerformanceModeUnitTest.laterHealingStrategyOverrideShouldWinOverMaximumPerformanceModeDisable (shaft-engine, added in PR #4173, issue #4161). TestNGListener.onStart(ISuite) (shaft-engine/src/main/java/com/shaft/listeners/TestNGListener.java:151) calls DriverFactoryHelper.preflightRemoteGridIfConfigured() -> initializeSystemProperties() -> PropertiesHelper.postProcessing() (guarded by a once-per-JVM compareAndSet, PropertiesHelper.java:204) at SUITE START, strictly before any @BeforeMethod. overridePropertiesForMaximumPerformanceMode() (PropertiesHelper.java:457-462) sets healing.strategy=disabled via the same ThreadLocalPropertiesManager-backed SHAFT.Properties.*.set() mechanism every other override uses. GeneratedTestValidator.replay() (shaft-capture) runs TestNG single-threaded, no parallelism, so suite-start and @BeforeMethod share one thread. Overrides are last-write-wins per thread, so ANY setUp()-emitted SHAFT.Properties.healing.set().strategy(...) call (e.g. a future generation-time healing wire-up) runs after and WINS -- silently re-enabling healing under maximumPerformanceMode 1/2, the opposite direction from 'generated tests silently lose healing.' Filed as a design question in follow-up issue #4172 for whoever wires shaft-capture into the fingerprint-seeding seam.