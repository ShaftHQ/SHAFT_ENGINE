CORRECTED 2026-07-26 (reviewer caught an overstated evidence claim before merge, PR #4173): the earlier wording said this ordering was "proven empirically" -- it was not. PropertiesHelperMaximumPerformanceModeUnitTest.laterHealingStrategyOverrideShouldWinOverMaximumPerformanceModeDisable (shaft-engine, PropertiesHelperMaximumPerformanceModeUnitTest.java:75-95) never instantiates TestNGListener or ISuite. It reflectively invokes the private overridePropertiesForMaximumPerformanceMode() directly, then sets healing.strategy in the same method and asserts last-write-wins on the ThreadLocalPropertiesManager-backed property store. That is true of any ThreadLocal-backed store regardless of TestNG; it does not exercise TestNGListener.onStart(ISuite) or the real chain through DriverFactoryHelper.preflightRemoteGridIfConfigured() -> initializeSystemProperties() -> PropertiesHelper.postProcessing().

What IS watched red/green: overridePropertiesForMaximumPerformanceMode() (PropertiesHelper.java:457-462) sets healing.strategy=disabled via SHAFT.Properties.healing.set().strategy(...); a later same-thread SHAFT.Properties.healing.set().strategy("shaft-heal") call overwrites it -- last-write-wins on that store, proven.

What is NOT watched by any test, and instead rests on TestNG's documented ISuiteListener contract plus a code-reading trace (citations accurate): that TestNGListener.onStart(ISuite) (TestNGListener.java:151) really fires before any @BeforeMethod in an actual running suite; that it really reaches PropertiesHelper.postProcessing() via preflightRemoteGridIfConfigured()/initializeSystemProperties(); and that GeneratedTestValidator.replay()'s subprocess really runs suite-start and @BeforeMethod on the same single, non-parallel thread. The chain is real and the citations check out, but no test in this repo exercises it end to end -- a real TestNG-suite-runner integration test (register TestNGListener, run an actual suite, assert property state from inside a real @BeforeMethod) would settle it conclusively and does not exist yet.

Net: IF the documented contract holds as traced, a generated test's setUp() that later sets healing.strategy would win over maximumPerformanceMode's forced disable -- the opposite direction from "generated tests silently lose healing". This is staged as a design input for follow-up issue #4172, which must treat it as evidence from a traced contract, not a proven runtime fact, and should settle it with a real suite-runner integration test before relying on it for a design decision.