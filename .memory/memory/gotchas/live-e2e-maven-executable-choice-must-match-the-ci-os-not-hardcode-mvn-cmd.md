intellij-tools-diagnostics and intellij-tools-core (.github/workflows/live-tools-nightly.yml:72,116) run on runs-on: ubuntu-22.04, which has no mvn.cmd file -- only mvn. ShaftAssistantPanelLiveDiagnosticsToolE2ETest hardcoded "mvn.cmd" (reasoning only about Windows CreateProcess/PATHEXT) and failed there with 'Maven command failed to launch'. HealerService.validationCommand (HealerService.java:349) already allowlists mvn, mvn.cmd, mvnw, and mvnw.cmd -- the test just needed to pick the one that exists for the current OS via a mavenExecutableForOs() helper. Fixed by commit 8f7137e882 (Fix #4021: use OS-appropriate Maven executable in healer/verify live E2E, merged via PR #4062). Rule: any live E2E test that spawns Maven via ProcessBuilder must select the executable per-OS, never hardcode one platform's name.