On PR #5042, unused Grid SPI parameters blocked merge via required_review_thread_resolution. Copying that pattern onto a NEW interface in a later PR still produces new PMD UnusedFormalParameter findings, because only the new file is in the PR diff. Dummy-use every unused SPI parameter with Objects.requireNonNull on default methods and overrides before the first push. Deleting the parameters is not the fix.