A crash dialog (RuntimeExceptionWithAttachments: 'Read access is allowed from inside read-action only') was reported as an IntelliJ IDEA plugin release blocker. Full visible stack trace is 100% inside IntelliJ Platform internals: DaemonCodeAnalyzerImpl.cancelAllUpdateProgresses -> cancelIndicator -> DaemonProgressIndicator.doCancel -> MyDaemonProgressIndicator.onCancelled -> message bus -> DaemonFusReporter.daemonCanceled/daemonStopped -> TrafficLightRenderer.getErrorCounts/getContext -> EditorContextManager.getEditorContext -> EditorContextManagerImpl.getEditorContexts/getCurrentContextStateWithPreferredDefault -> AtomicMapCache.computeIfAbsent -> CodeInsightContextManagerImpl.getPreferredContext -> ThreadingAssertions.softAssertReadAccess. No com.shaft.intellij frame appears anywhere in the captured trace. The offending thread is named 'DefaultDispatcher-worker-74' (a Kotlin kotlinx.coroutines Dispatchers.Default pool thread name).

Verified shaft-intellij (platformVersion=2024.3, pluginSinceBuild=243) applies zero Kotlin Gradle plugin, has zero .kt source files, and zero kotlinx-coroutines dependency anywhere in build.gradle.kts/gradle.properties (checked twice, independently). Also grepped all of shaft-intellij/src/main: zero references to DaemonCodeAnalyzer, .restart(), or cancelAllUpdateProgresses anywhere in plugin code. Every plugin code path that touches Editor/Document after an async MCP CompletableFuture callback correctly hops back to the EDT first via ApplicationManager.getApplication().invokeLater(...) before touching editor state (e.g. PickLocatorAtCaretAction.actionPerformed, GuidedWorkflowPanel's onEdt() helper, ShaftAssistantPanel) -- no unguarded background-thread editor/PSI access exists in this plugin.

CORRECTION (same investigation, second pass): the claim above that shaft-intellij "cannot itself spawn a DefaultDispatcher-worker thread" because it has no Kotlin/coroutines dependency is wrong. A plain-Java class implementing a platform-provided Kotlin suspend interface -- e.g. com.intellij.openapi.startup.ProjectActivity#execute(Project, Continuation) -- is invoked BY THE PLATFORM'S OWN Dispatchers.Default coroutine scope, on exactly a DefaultDispatcher-worker-NN thread, with zero Kotlin/coroutines dependency required in the implementing module. shaft-intellij added exactly one such class in the same release window that regressed (10.3.20260714, last accepted -> 10.3.20260715, marketplace-rejected): ShaftPluginUpgradeActivity (postStartupActivity, PR #3587), the only new code in that diff that runs via a coroutine dispatcher thread -- matching the crash's DefaultDispatcher-worker-74 thread class. The full stack above DaemonCodeAnalyzerImpl.cancelAllUpdateProgresses was not visible in the reported screenshot, so direct causation (this activity's execution being the literal caller of the daemon cancel) is NOT proven, only correlated by being the sole newly-added coroutine-dispatcher code in the regressed diff.

Conclusion: the crash's visible frames are still 100% IntelliJ Platform-internal (DaemonFusReporter's daemonCanceled/daemonStopped listener calling TrafficLightRenderer.getErrorCounts()/getContext() -> CodeInsightContextManagerImpl.getPreferredContext() without wrapping in a ReadAction) -- this remains a real platform-side threading bug in the CodeInsightContext 'multiverse' subsystem (new in 2024.3), not a plugin logic bug. But "no plugin code needed changing" was premature: shaft-intellij's ShaftPluginUpgradeActivity.execute() ran its full body (settings/credential/tool-approval reset) inline on the platform's shared startup coroutine dispatcher thread, which is unnecessary and only serves to keep the activity alive longer on exactly the thread class implicated in the crash. Applied mitigation: execute() now only schedules the work via ApplicationManager.getApplication().invokeLater(...) and returns immediately, minimizing this activity's footprint on the coroutine dispatcher (shaft-intellij commit following PR #3608, branch codex/3608-followup-upgrade-activity-edt-hop). This is a safe, idiomatic hardening regardless of whether it is the true root cause; it cannot be verified as *the* fix without a marketplace re-submission (rejections happen at JetBrains' automated verification stage, not in our own CI, which never launches a live IDE). If the next marketplace submission is rejected with the same signature, escalate to JetBrains via youtrack.jetbrains.com (component: Platform / Code Insight, citing DaemonFusReporter.kt:78/108 and CodeInsightContextManagerImpl.kt:123) -- but do not skip re-auditing every ProjectActivity/coroutine-dispatcher entry point first, since that is the one plugin-side lever proven to exist here.