Hit independently by two separate agents in one day during shaft-intellij live-E2E work (live-tools-nightly.yml artifact names confirmed: intellij-live-tools-core-evidence, intellij-live-tools-diagnostics-evidence, intellij-live-tools-authoring-evidence, intellij-live-gemini-evidence at live-tools-nightly.yml:107,149,207,266). Gradle's console test-failure summary truncates long assertion messages, so reading only the console log or GH Actions log view loses the actual payload (e.g. the real Gemini raw-response text or the full JSON parse error). The untruncated text lives in the run's uploaded `*-evidence` artifact's HTML test report -- fetch it with `gh run download <run-id> -n <artifact-name>` and open the HTML report, not the console log, whenever a live-E2E assertion failure's exact text matters for diagnosis.