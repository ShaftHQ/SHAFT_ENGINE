Probed against real binaries. Both `claude -p` and `codex exec -` read prompts from stdin and print their final messages to stdout, but Codex on Windows can interleave process-termination lines around the result; parse output tolerantly by scanning for the expected structured payload (see `extract_repair_json` in `shaft-upgrader/upgrade_to_modular_shaft.py`). Copilot CLI non-interactive behavior remains unverified and deliberately unsupported in `AgentCliRepairClient`. During harness smoke verification, Codex 0.146.0 with `-s read-only` allowed SessionStart but failed PowerShell creation with `CreateProcessWithLogonW failed: 2`; vetted automation that must read from disk can use the explicit unsandboxed automation flag only after authorization. Claude 2.1.221 counts initial context-cache creation against `--max-budget-usd`: a $0.20 cap stopped after a successful Read because cache creation cost $0.34539, before the final response. For a harness smoke, either accept the hook response plus successful tool result as evidence or set the cap above initial cache cost.