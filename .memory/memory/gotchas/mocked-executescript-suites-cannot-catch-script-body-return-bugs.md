JavaScriptWaitManagerUnitTest/BidiNetworkActivitySourceUnitTest mock executeScript's return value, so a JS snippet that evaluates to null in a real browser (e.g. BROWSER_READINESS_STATE missing its top-level 'return' before the IIFE -- fixed in #3749 increment D) stays green in unit tests while being a production no-op. Selenium wraps script text as a function body: every snippet in JavaScriptHelper must begin its result expression with 'return'. Any new composite readiness JS needs at least one live headless-browser assertion (LazyLoadingFixtureLiveTest pattern) that the script returns non-null.