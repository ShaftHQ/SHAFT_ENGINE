Durable native Memory under .memory/ (config, events, schema, memory/, relations/) must be git-tracked. The installer .gitignore previously allowlisted only a few files and ignored new gotchas until git add -f. ChaosEngine now ignores only runtime/private paths (.memory/index, context, exports, recovery, .backup, .lock, private) and graphify-out / MemPalace derived indexes. Ordinary git add is enough; force-add is not the workflow. Keep retrieve-first active for Memory, MemPalace, and Graphify before broad discovery. Issue #5774.
