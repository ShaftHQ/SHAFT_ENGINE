Investigated during #4086 orchestration to rule out a hypothesis, filed so nobody re-derives it. GLOBAL_TESTING_SCOPE is defined as a workflow-level env in both .github/workflows/e2eLocalTests.yml:16 and .github/workflows/e2eTests.yml:42, and it IS genuinely consumed via -Dtest=${{ env.GLOBAL_TESTING_SCOPE }} / -Dtest=${GLOBAL_TESTING_SCOPE} at e2eLocalTests.yml:72,103,138,314 and e2eTests.yml:248,285,322 (the 7 desktop-browser-matrix jobs). It is NEVER consumed by any pom.xml or Java source -- that part is expected, it is only ever a shell/YAML CLI-arg interpolation, not a defect by itself. The trap: every OTHER job in both files (Sikuli Windows Desktop e2eLocalTests.yml:171, WindowsDesktopAppiumE2ETest e2eLocalTests.yml:263, the Cucumber job e2eLocalTests.yml:350, and in e2eTests.yml the DB/API/Visual/Video jobs, all 5 Playwright-per-browser jobs, all BrowserStack jobs, the Cucumber-remote job, and the final Junit*,MoonTests job at e2eTests.yml:859) hardcodes its own literal -Dtest string and never interpolates GLOBAL_TESTING_SCOPE at all. Seeing the variable defined at file scope makes it look like a blanket exclusion contract that applies everywhere; it does not -- check each job's actual -Dtest value, don't assume the global exclusion list applies just because the var exists in env:.