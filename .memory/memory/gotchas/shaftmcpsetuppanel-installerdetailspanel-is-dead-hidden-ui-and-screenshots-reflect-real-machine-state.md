installerDetailsPanel (manual target combo + installer command text area) is setVisible(false) with no toggle since the #3270 onboarding simplification - controls added there are invisible to users. The real user surface for installer-command changes is the step-3 Copy command row (installActions), whose visibility follows showCopy in updateActionState; showCopy is false when an installed shaft-mcp is detected, so tests must not gate enabled-state on showCopy (doClick becomes a no-op on dev machines with shaft-mcp installed). Clean-state screenshot rendering: pass -Dshaft.intellij.mcp.applicationDataRoot and -Dshaft.intellij.mcp.bootstrapRoot pointing at an empty dir (forwarded through the Gradle test JVM since PR #3446); blank means use-real-default.