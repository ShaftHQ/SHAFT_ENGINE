AGENTS.md only calls out refreshing shaft-engine's local .m2 install after touching shaft-engine, but shaft-mcp also depends on shaft-capture, and that jar can be independently stale. Hit during #3350 part 4: shaft-capture 10.3.20260711 in .m2 had CaptureNetworkRecorder.matchesGlob as PRIVATE even though the checked-out source (post part-1 commit c6af018b7d) already made it public, and CaptureStartOptions was missing the saveHarContent-arg constructor added in part 3 -- a stale cached jar under the same date-based version string, not a deliberate shadow. Fix: run `mvn -pl shaft-capture install -DskipTests -Dgpg.skip=true` in addition to the shaft-engine install whenever a change spans shaft-engine + shaft-capture + shaft-mcp, before compiling/testing shaft-mcp.