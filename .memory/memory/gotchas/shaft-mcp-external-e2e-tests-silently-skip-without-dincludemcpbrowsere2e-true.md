shaft-mcp/pom.xml:276 sets <excludedGroups>external-e2e</excludedGroups> by default, so a scoped `mvn test -pl shaft-mcp -Dtest=EngineServiceTest#<method>` runs 0 tests without error. The mcp-browser-e2e profile (pom.xml:285-313, activated by -DincludeMcpBrowserE2E=true) overrides it via combine.self="override". Working command shape: mvn test -pl shaft-mcp '-Dtest=EngineServiceTest#<method>' '-DheadlessExecution=true' '-Dallure.automaticallyOpen=false' '-DincludeMcpBrowserE2E=true'. Always confirm the surefire report shows Tests run >= 1, not skipped. Mirrors the capture-side gotcha (includeCaptureBrowserE2E).