Repeated evaluation of `this::methodName` is not guaranteed to return identity-equal objects. Listener removal by object identity can silently fail when add/remove use separate method-reference expressions. Capture the listener once in a field and pass the same instance to both operations. This is also recorded in `.agents/skills/act-as-mohab/references/shaft-mastery/intellij-plugin.md`.