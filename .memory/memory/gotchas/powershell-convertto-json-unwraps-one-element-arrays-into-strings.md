On 2026-08-17 review of PR #5063, run_agent.ps1 wrote report.json via ConvertTo-Json. A single-file allowlist became a JSON string, so validate_report treated files_allowed as missing and skipped files_changed when it was also a string. Fix: coerce strings to one-element lists in agent.as_path_list, and keep @() wrappers in the payload. Evidence: tests.scripts.test_local_coding_agent.LocalCodingAgentReportTest.test_powershell_unwrapped_* and commit 0f80955e74.