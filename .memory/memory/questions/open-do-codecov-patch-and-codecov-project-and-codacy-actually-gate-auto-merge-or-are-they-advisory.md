Not proven either way -- record as a question, not a conclusion. Since `PR Gate Summary` is main's only required status check (see fact.main-branch-required-status-check-is-pr-gate-summary-via-rulesets-api) and codecov/patch + codecov/project are third-party GitHub App checks rather than jobs inside the pr-gate.yml workflow, PR Gate Summary cannot literally depend on their pass/fail -- which implies they are advisory and do not by themselves block auto-merge. Observed 2026-07-26 on PR #4104: both codecov checks failed while the PR remained mergeable-pending (auto-merge did not un-arm). NOT confirmed: whether Codacy gates the merge directly as its own required check, or only indirectly via whatever job(s) `PR Gate Summary` aggregates -- do not assert either way until checked. Same lesson as gotcha.auto-merge-armed-on-pr-gate-green-is-unsafe-when-the-fixs-acceptance-criterion-is-a-job-outside-the-gate from the other direction: that gotcha is 'the proving job may be outside the gate', this question is 'which checks are even inside the gate in the first place'.