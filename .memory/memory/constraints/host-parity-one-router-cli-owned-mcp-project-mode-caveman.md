# Host parity load path and CLI-owned MCP (#5698)

One in-project ChaosEngine router body lives at `chaos-engine/skills/chaos-engine/SKILL.md`. Host skill files and role adapters are generated pointers only; they must not restate Iron laws or the Operating contract.

Companion skill bodies are catalog rows plus SessionStart locators; do not load vendor bodies by default.

When `gh` is the owned GitHub client, doctor must fail if any GitHub MCP alias (`github`, `github-gh`, `github_gh`) is enabled. Same for Graphify MCP when the Graphify CLI is owned. Heal prompt: `No duplicate GitHub MCP. Repair: disable extras in host MCP config.`

Caveman project mode: if a ChaosEngine router exists in the directory walk, never fall through to user-level caveman config; default intensity is ultra.
