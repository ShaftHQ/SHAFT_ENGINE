When any task in this repo surfaces something outside its narrow scope: fix blockers/small issues (breaks the code being changed, or a low-risk few-line fix) inline as part of the current change -- never leave a known-broken path in place just because it's technically out of scope. Every medium/large issue or enhancement idea noticed must be captured through the AGENTS.md Learning Loop before Completion: search existing GitHub issues first, then file or update a followup issue, consolidating related items into one issue (never one issue per finding), listing each item with file/method, root cause or opportunity, evidence, and suggested approach. Never silently drop a noticed item. This is opportunistic only: act on what the task's own footprint actually surfaces, never proactively hunt for extra issues. Every bug fixed inline should still be mentioned in the PR/commit description; security-relevant or data-loss bugs fixed inline are still worth their own retrospective issue for audit trail purposes -- the one exception to consolidation.