`.agents/skills/act-as-mohab/SKILL.md` routes retrieval for every task: native `.memory/` for durable constraints and gotchas, MemPalace for cross-session RAG and impact, Graphify for current structure, then targeted live-file verification. Use `tools/repository-map/resolve_graph_out.py --check` when available; never rebuild Graphify from a worktree. If a source is unavailable, record degraded mode and continue with the remaining sources.