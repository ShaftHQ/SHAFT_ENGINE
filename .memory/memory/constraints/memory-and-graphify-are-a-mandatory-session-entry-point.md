`.memory/` (via `memory search`/`memory load "<task>"`/`memory remember --stdin`) and `.claude/skills/graphify` are a mandatory entry point for every SHAFT_ENGINE session, not a proactive-if-convenient preference -- both are git-committed specifically so they persist across every future session/worktree/machine. Run `memory load "<task>"` or `memory search` at the very start of a session, before other exploration, regardless of task size. Save durable gotchas/decisions/workflows via `memory remember --stdin` as they're discovered, not only when asked at the end. Before a broad multi-file exploratory search, check whether the graphify cache (`tools/repository-map/resolve_graph_out.py --check`) exists and would be faster than manual Grep/Read traversal -- but never rebuild it from a worktree, only from the main checkout. If a worktree is missing `.memory/` or `.claude/skills/graphify/SKILL.md`, that's a signal to sync from origin/main, not proceed without them.