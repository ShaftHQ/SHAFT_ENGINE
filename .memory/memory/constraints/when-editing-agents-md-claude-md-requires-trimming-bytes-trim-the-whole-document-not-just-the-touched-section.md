If an instruction-doc edit needs to shrink to fit a length/byte budget, do a trimming pass across the ENTIRE document (AGENTS.md, CLAUDE.md, or whichever canonical instructions file), not just the section being changed right now. Reason given by the user: these docs are already written in a dense, byte-conscious style, and localized trimming lets stale verbosity accumulate elsewhere while the touched section gets squeezed disproportionately. This only applies when a trim is actually needed (e.g. approaching a documented length cap) -- a small net-positive addition, like naming an exact flag, does not itself trigger a full-document trim.