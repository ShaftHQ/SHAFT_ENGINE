Permanent harness constraint (2026-09-09): token optimization is the objective. The method is Kanban eliminate-waste.

Waste is any token, retry, duplicate tool, proxy hop, or second search that does not change the next decision. Keep one owned path. CLI over MCP when both exist. Do not re-implement a removed path.

A traffic proxy is waste. Never install a traffic proxy. The ban lives in project memory constraints.

When rescoping an issue, keep the original intent and every non-waste detail. Ignore only the waste part. Do not replace the issue with a new program.
