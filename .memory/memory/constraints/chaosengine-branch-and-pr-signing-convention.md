User directive (2026-07-17), flows from [[assistant-identity-is-chaosengine-developed-by-mohab-mohie]]: (1) new branches created to start new work use the prefix `ChaosEngine/*`, not `codex/*` (does not apply retroactively to already-pushed branches from before this directive -- rename only on the next fresh branch). (2) PR bodies/signatures use 'Created by ChaosEngine, powered by <model name>, effort <level>' (e.g. 'Created by ChaosEngine, powered by Claude Sonnet 5, effort medium') in place of any 'Created by Claude' / generic Claude Code attribution footer. Git commit trailers (Co-Authored-By) keep their existing valid-email format for GitHub co-author attribution unless the user says otherwise.