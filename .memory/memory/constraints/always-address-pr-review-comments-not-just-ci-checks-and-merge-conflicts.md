User correction (2026-07-17): before merging any PR, read and address actual review feedback -- bot annotations (Codacy, CodeQL, etc.) and human review comments -- not only whether checks are green and conflicts are resolved. Caught on PR #3654: Codacy's check-run annotations (unused import, NPath complexity 2376 in DoctorEvidenceImageLocator.imagePathsFrom) were initially left unaddressed even though the failing build-and-verify test (a separate, unrelated icon-only-buttons regression) was the only thing chased down. How to apply: when a PR's checks include a static-analysis/Codacy/CodeQL run, fetch its check-run annotations (`gh api repos/OWNER/REPO/check-runs/<id>/annotations`) and its issue comments (`gh pr view <n> --json comments`) and fix what they flag before considering the PR ready to merge, in addition to making CI green.