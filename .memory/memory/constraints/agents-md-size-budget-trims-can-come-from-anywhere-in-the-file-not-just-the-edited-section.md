User note (2026-07-17): validate_agent_setup.py enforces a hard byte/token budget on AGENTS.md (and an aggregate agent-guidance budget across harness files) with very little headroom. When an edit needs trimming to fit, the cut does not have to come from the section just edited -- trim whichever existing wording anywhere in the file has the least informational value/impact, then re-run `py -3 scripts/ci/validate_agent_setup.py` to confirm. Don't over-index on preserving every word of an unrelated section if the section being added is more valuable.