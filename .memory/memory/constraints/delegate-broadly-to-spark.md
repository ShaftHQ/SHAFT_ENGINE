For SHAFT work, delegate tasks to lower-capability agents such as gpt-5.3-codex-spark whenever they are available, responsive, and useful for bounded parallel work; no separate user permission is needed. Check whether lower-intelligence models count against the same usage counter as higher-intelligence models. Always delegate to the highest-intelligence available and responsive model that counts toward the cheapest applicable usage counter. Use another lower-capability available agent only if Spark is unavailable, unresponsive, usage-limited, or not the cheapest-counter choice. The main agent must always own the task: orchestrate delegation, review outputs, consolidate results, make final decisions, and remain responsible for correctness. User instruction on 2026-06-24: always delegate tasks to lower intelligence agents like Spark when available and responsive, but always own the tasks yourself; also check shared usage counters and prefer the highest intelligence model on the cheapest counter.