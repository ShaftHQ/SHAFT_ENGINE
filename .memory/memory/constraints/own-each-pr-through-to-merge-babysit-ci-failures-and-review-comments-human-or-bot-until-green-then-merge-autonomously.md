For the #3643 umbrella program (and by extension any multi-PR autonomous task under this contract), opening a PR is not the finish line. After opening each phase PR, keep watching it: fix any CI failures, address any review comments left by humans or bots, re-push, and only stop once every check is green and comments are resolved -- then merge the PR autonomously without waiting for further user confirmation. Why: the user explicitly said 'you need to babysit each pr you open to fix any ci issues or any review comments left by humans or bots until everything is green. then merge the pr' mid-task on 2026-07-17, extending the earlier one-PR-per-phase contract (which had only covered opening a 'ready' PR, leaving merge timing ambiguous) into full lifecycle ownership including the merge action itself. How to apply: treat 'PR opened' as an intermediate state, not a completion state, in task tracking; after opening a PR, periodically check its CI status (gh pr checks) and review threads (gh api .../comments, gh pr view --comments) until resolved; this is standing authorization to merge (not just open) PRs opened under this contract once genuinely green -- do not re-ask before each individual merge. Still exercise judgment: a red CI run needs root-caused fixing, not a bypass, and a substantive human review comment needs a real response, not a rubber-stamp resolve. Related: [[fable-advisor-consultant-for-architecture-decisions]].