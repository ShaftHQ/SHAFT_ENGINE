The user re-emphasized mid-task (2026-07-17, alongside the PR-babysitting instruction) to 'use fable as a consultant and architect when needed' -- i.e. actively invoke mcp__gbrain__advisor (or apply act-as-fable's methodology explicitly, e.g. its live-probe-before-implementing and front-load-the-riskiest-unknown principles) when facing a genuine architectural decision or design uncertainty, not only apply it implicitly as background discipline. Why: this was part of the original task's standing instruction ('use Fable as your advisor and ask it for guidance or advice or validation as needed') but the user chose to restate it, signaling it should be applied more visibly/deliberately going forward. How to apply: before committing to a non-obvious implementation design (e.g. an unclear architectural fork, a spec with real trade-offs), consider an explicit gbrain advisor consult or a stated act-as-fable-style scouting step, not just silent internal reasoning. Related: [[own-each-pr-through-to-merge-babysit-ci-failures-and-review-comments-human-or-bot-until-green-then-merge-autonomously]].