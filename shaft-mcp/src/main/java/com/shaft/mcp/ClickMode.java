package com.shaft.mcp;

/**
 * Click gesture requested from the unified {@code element_click} tool.
 */
public enum ClickMode {
    /** A single, ordinary click (the default). */
    SINGLE,
    /** A double click / double tap. */
    DOUBLE,
    /** A click-and-hold / long tap. */
    LONG
}
