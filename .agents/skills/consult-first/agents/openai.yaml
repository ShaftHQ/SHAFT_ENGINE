interface:
  display_name: "Consult"
  short_description: "Size the work and pick an approach first"
  default_prompt: "Use $consult-first to choose an approach for this change."
policy:
  allow_implicit_invocation: true
