<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.shafthq</groupId>
        <artifactId>shaft-parent</artifactId>
        <version>10.2.20260614</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>SHAFT_ENGINE</artifactId>
    <packaging>pom</packaging>
    <name>${project.groupId}:${project.artifactId}</name>
    <description>Legacy SHAFT coordinate retained as a relocation POM.</description>

    <distributionManagement>
        <relocation>
            <groupId>${project.groupId}</groupId>
            <artifactId>shaft-engine</artifactId>
            <version>${project.version}</version>
            <message>SHAFT Engine moved to io.github.shafthq:shaft-engine. Use the preferred automated upgrade: https://shaftengine.netlify.app/docs/start/upgrade</message>
        </relocation>
    </distributionManagement>
</project>
